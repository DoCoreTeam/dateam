# PLAN newAX: 공공데이터포털 여섯 서비스를 등록하고 신청 상태를 말한다
플랜 ID: P0012
플랜 버전: v0.1.0
상태: 완료
지시: ins_0007
목표 버전: v0.10.60
작성: 2026-09-16
시작 커밋: ce367a79

## 목표
- 같은 서비스 키로 여섯 서비스가 열리는데 코드에는 하나만 있다
- 신청 안 된 서비스를 부르면 지금은 나라장터가 응답하지 않는다고 말한다, 사실은 우리가 신청을 안 한 것이다
- 서비스마다 신청 상태와 일일 한도와 마지막 결과를 화면에서 본다

## 범위 밖
- 실제 신청, 그것은 사람이 포털에서 하는 일이다
- 사전규격 수집을 레이더에 실제로 잇는 일, 이번은 등록과 상태까지
- 카탈로그 신설 (9단계)

## 완료 정의
- pnpm tsc --noEmit, pnpm lint, pnpm test, pnpm build 통과
- 여섯 서비스가 한 곳에 등록되고 각각 무엇을 주는지 적혀 있음
- 신청 안 됨과 한도 초과와 진짜 고장을 구분해 말함
- 서비스 키가 없는 지금 상태를 화면이 정확히 말함

## 참조
- docs/2026-09-10-ai-common-layer/README.md 8절 공공데이터포털, 12절 놓치기 쉬운 것 10
- apps/web/lib/rfp/g2b/client.ts (사유마다 다음 행동을 적어 둔 실물)

## 항목

### I01 서비스 여섯 등록과 신청 상태 구분
상태: 통과
모드: 경량
범위: apps/web/lib/rfp/g2b/services.ts (신규), apps/web/lib/rfp/g2b/services.test.ts (신규), apps/web/lib/rfp/g2b/client.ts, apps/web/package.json
감사 기준:
- 여섯 서비스(입찰공고 사전규격 낙찰 계약 계약과정통합 누리장터)를 id 와 주소와 무엇을 주는지와 함께 등록
- 지금 쓰는 것이 무엇이고 안 쓰는 것이 무엇인지 목록이 말함
- 신청 안 됨을 따로 된 사유로 두고, 그때 할 일이 「신청하세요」인 것을 화면 문구로 적음
- 포털 오류 코드를 사유로 옮기는 함수와 단정, 실제 호출로는 검증 못 하는 이유를 notes 에 적음
- node --test 통과, 등재 후 총 테스트 수 증가 확인
의존: 없음

### I02 서비스 상태 화면
상태: 통과
모드: 경량
범위: apps/web/app/(rfp)/rfp/admin/G2bServices.tsx (신규), apps/web/app/(rfp)/rfp/admin/page.tsx, apps/web/lib/rfp/terms.ts
감사 기준:
- 여섯 서비스가 이름과 무엇을 주는지와 지금 쓰는지로 보임
- 서비스 키가 없으면 그 사실을 먼저 말하고 나머지를 흐리게 두지 않음
- 화면에 적는 말은 용어집을 지남
- 아직 못 하는 것을 할 수 있는 것처럼 두지 않음 (신청은 포털에서 하는 일이라고 말함)
- tsc 통과
의존: I01

### I03 서비스 등록 가드
상태: 통과
모드: 경량
범위: apps/web/lib/policy/g2b-service-guard.test.ts (신규), apps/web/package.json
감사 기준:
- 포털 주소를 코드에 직접 적으면 실패, 등록부를 지나야 함
- 등록부의 서비스마다 무엇을 주는지가 비어 있지 않음
- 일부러 깨서 둘 다 확인
- 규칙이 도는 대상이 0개면 실패하는 단정
- 등재 후 총 테스트 수 증가 확인
의존: I02

## 종합 감사

실행한 명령과 결과 (2026-09-16)

- pnpm tsc --noEmit 통과, 오류 0건
- pnpm lint 통과, exit 0
- pnpm test 5614건 전부 통과, 실패 0
- pnpm build 통과

완료 정의 대조

- 여섯 서비스가 한 곳에 등록되고 각각 무엇을 주는지 적혀 있음
- 신청 안 됨과 한도 초과와 키 문제와 모름을 넷으로 갈라 말함, 할 일도 넷이 다름
- 서비스 키가 없는 지금 상태를 화면이 먼저 말함

전체 diff 검토

- 시작 커밋 ce367a79, 항목 커밋 3개 (v0.10.61 부터 v0.10.65)
- 범위 밖 변경 0건
- diff 에 키 토큰 비밀번호 0건, 화면은 키가 있는지만 받고 값은 안 받음

발견 사항 (계획에 없던 것 둘)

- 서비스 키가 DB 에 아직 없다. 나라장터 연동이 지금 아무것도 안 돌고 있다
- 기존 시험이 모든 실패 안내에 「지금 당장 할 수 있는 것」을 요구했고 그것이 옳았다. 신청하세요에 첨부를 직접 올리라는 말을 더했다

남은 것

- 실제 활용 신청은 사람이 포털에서 하는 일
- 키가 생기면 한 번 불러 오류 코드 표가 실제와 맞는지 확인해야 한다 (지금은 포털 문서 기준)
- 사전규격을 레이더에 실제로 잇는 일

## 변경 이력
- v0.1.0 (2026-09-16) 최초 작성 (ins_0007)
