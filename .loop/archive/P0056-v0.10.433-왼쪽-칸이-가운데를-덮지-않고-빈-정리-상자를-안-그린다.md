# PLAN newAX: 왼쪽 칸이 가운데를 덮지 않고 빈 정리 상자를 안 그린다
플랜 ID: P0056
플랜 버전: v0.1.1
상태: 완료
지시: iv_0108
목표 버전: v0.10.433
작성: 2026-09-23
시작 커밋: e36716b3

## 목표
- 미팅 상세에서 왼쪽 「이 미팅은」 카드가 제 칸을 넘어 가운데 「회의 기록」을 덮는 것을 없앤다
- 정리한 것이 없을 때 큰 빈 상자를 안 그려서 아래 쓰는 자리가 위로 올라온다

## 범위 밖
- RecordLayout 3슬롯을 2슬롯으로 이관하는 일 (뜻이 뒤집혀 있어 재배치가 필요, 이번 일이 아님)
- 저장 실패 문구와 자동 저장 동작
- 회의노트 화면(/meeting-notes)의 다른 카드 배치

## 완료 정의
- pnpm tsc --noEmit, pnpm test, pnpm build 통과
- 1760px 폭에서 왼쪽 칸 안의 어떤 요소도 칸 오른쪽 경계를 넘지 않음 (실측)
- 정리본이 없으면 「아직 정리하지 않았어요」 상자가 화면에 없음
- 사용자 노출 문자열은 용어집 상수 사용

## 참조
- apps/web/components/ui/crm/record-layout.module.css (레코드 열 골격)
- apps/web/components/meeting/MeetingDigestPanel.tsx (정리 자리)
- LOOP.md 7절 보안 세 질문

## 항목

### I01 카드가 제 칸을 안 넘는다
상태: 통과
모드: 경량
범위: apps/web/components/ui/crm/record-layout.module.css, apps/web/app/(crm)/crm/meetings/[id]/meeting-facts.module.css, apps/web/lib/policy/record-layout-guard.test.ts(신규), apps/web/package.json
감사 기준:
- 보안: 새 데이터도 새 창구도 없고 밖에서 온 값도 안 다룬다. 칸 폭 계산만 바뀐다. 해당 없음
- headless chromium 실측에서 1760px 폭 3열 배치의 왼쪽 카드 폭이 300px 이고 칸 경계를 넘는 요소가 0개 (고치기 전 370px, 넘는 요소 있음)
- 날짜와 시각 두 칸이 좁은 칸에서 한 줄로 접힌다 (grid-template-columns 계산값이 한 열)
- record-layout-guard.test.ts 가 열 클래스 넷에 minmax(0, 1fr) 이 있는지 보고, 규칙을 지운 판으로 일부러 깨뜨려 실패를 확인
- pnpm tsc --noEmit 통과
의존: 없음

### I02 정리한 것이 없으면 정리 상자를 안 그린다
상태: 통과
모드: 경량
범위: apps/web/lib/meeting/digest-slot.ts(신규), apps/web/lib/meeting/digest-slot.test.ts(신규), apps/web/components/meeting/MeetingDigestPanel.tsx, apps/web/components/meeting/digest.module.css, apps/web/lib/meeting/digest-progress.test.ts, apps/web/package.json
감사 기준:
- 보안: 위와 같다. 해당 없음
- digestSlot 이 정리본 있음이면 result, 없고 재료 있고 고칠 수 있으면 run, 그 밖은 none 을 준다
- digest-slot.test.ts 가 여섯 갈래를 다 밟고 pnpm test 에 등재돼 실제로 돈다 (등재 후 총 테스트 수가 는다)
- MeetingDigestPanel 이 none 일 때 EmptyState 를 안 부른다 (정리본 없는 회의에서 「아직 정리하지 않았어요」 문자열이 화면에 없음)
- 기존 가드 digest-progress.test.ts 의 분기 앵커를 새 조건으로 옮기고 그 가드가 다시 통과한다
- pnpm tsc --noEmit 통과
의존: I01

## 종합 감사
- typecheck `pnpm tsc --noEmit`: 오류 395건, **전부 TS6053**(남의 세션이 tsconfig 에 넣은 `.next-p54` `.next-p0044` `.next-p0045` `.next-e2e-p0042` `.next-verify` 의 생성 타입 폴더가 지워져 있음). TS6053 아닌 오류 0건, 내 범위 파일이 오류에 나온 횟수 0건
- lint `pnpm lint`: 통과 (경고만, 전부 기존 것)
- test `pnpm test`: 6796개 전부 통과, 실패 0
- build `NODE_OPTIONS=--max-old-space-size=8192 NEXT_DIST_DIR=.next-p0056 pnpm build`: 통과. 기본 힙으로 돈 첫 판은 «JavaScript heap out of memory» 로 죽었는데 코드 오류가 아니라 동시에 도는 다른 세션과 겹친 메모리 부족이었음. 끝나고 `.next-p0056` 지움
- 보안 다섯 줄 (docs/policy/security-count.sql 실행): rls_off_tables 0, anon_write_tables 0, public_using_true_policies 0, unpinned_secdef_functions 0, anon_readable_secdef_views 0 — 전부 0
- 전체 diff `git diff 3ee6d64e..HEAD --stat`: 파일 8개, 235 추가 22 삭제. 범위 밖 파일 없음, 비밀 문자열 0건, 한자 0건
- 완료 정의 대조
  - 1760px 폭 실측: 왼쪽 칸을 넘는 요소 0개, 왼쪽 카드 오른쪽 끝 340 < 가운데 카드 왼쪽 356 (고치기 전 넘는 요소 3개, 시각 칸이 51px 밖으로 나가 391 에서 가운데를 덮었음)
  - 정리본이 없으면 「아직 정리하지 않았어요」 상자가 안 그려짐 — 가드가 `<EmptyState` 를 패널에서 0건으로 확인하고, 되살린 판으로 일부러 깨뜨려 실패를 봤음
  - 화면 문구는 기존 용어집 상수만 씀 (새 문자열 0개, DIGEST_EMPTY_TITLE 은 이 패널에서 안 쓰게 되어 들여오기에서 뺌)
- 항목 대 결과 대조: I01 의 네 파일과 I02 의 여섯 파일이 커밋 43bfcc00 · ee2cf66f 에 실제로 들어감
- 발견 사항 — 「이 미팅은」의 빈 상태 문구는 `/meeting-notes` 의 MeetingReadBody 에도 있으나 그쪽은 **사용자가 직접 연 탭 안**이라 그대로 둠. 탭을 열었는데 아무것도 없으면 탭이 고장난 것으로 읽힌다. 늘 떠 있는 자리와 성격이 다르다

## 변경 이력
- v0.1.0 (2026-09-23) 최초 작성 (iv_0108)
- v0.1.1 (2026-09-23) 빈 상태 분기를 바꾸니 기존 가드 digest-progress.test.ts 가 앵커('!latest ?')를 못 찾아 멈췄다. 가드가 조용히 통과하지 않고 멈춘 것이 맞는 동작이라, 그 앵커를 새 조건으로 옮기는 일을 I02 범위에 넣는다 (audit:I02)
