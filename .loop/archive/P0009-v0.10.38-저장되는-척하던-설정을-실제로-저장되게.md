# PLAN newAX: 저장되는 척하던 설정을 실제로 저장되게
플랜 ID: P0009
플랜 버전: v0.1.0
상태: 완료
지시: ins_0007
목표 버전: v0.10.38
작성: 2026-09-16
시작 커밋: aed226b7

## 목표
- 스위치를 켜도 아무 데도 안 남는 설정 화면이 있다, 사용자는 켠 줄 알고 우리는 안 켜진 채로 돈다
- 저장되는 척하는 것을 먼저 고친다, 없는 화면을 새로 만드는 것보다 이것이 급하다
- 저장이 안 되는 설정이 새로 생기면 가드가 막는다

## 범위 밖
- 아예 없는 화면 여섯 전부를 신설하는 일, 이번은 저장되는 척하던 것과 표는 있는데 화면이 0인 것 하나까지
- 카탈로그 신설과 능력별 프롬프트 화면 (9단계)
- 공공데이터포털 서비스 상태 화면 (8단계)

## 완료 정의
- pnpm tsc --noEmit, pnpm lint, pnpm test, pnpm build 통과
- 화면에서 바꾼 설정이 새로고침 뒤에도 남음
- 저장 창구 없이 바꿀 수 있는 설정 화면이 0개, 가드가 확인
- 설정값을 env 로 넣지 않음

## 참조
- docs/2026-09-10-ai-common-layer/README.md 10절 설정 화면 열넷, 11절 5단계
- apps/web/components/rfp/RuleSettings.tsx (스위치는 있고 창구가 없는 실물)

## 항목

### I01 저장되는 척하는 화면을 전수로 잰다
상태: 통과
모드: 경량
범위: docs/2026-09-10-ai-common-layer/step5-measure.md (신규)
감사 기준:
- 설정 화면마다 바꿀 수 있는지와 저장 창구가 있는지를 표로, 파일과 줄을 짚음
- 실측 기준선: RuleSettings 42줄이 화면 상태만 바꾸고 저장 요청이 0건
- 표는 있는데 화면이 0인 것과 화면은 있는데 창구가 0인 것을 구분해 셈
- 어느 것이 급한지 근거와 함께 적음 (속이는 쪽이 먼저)
- 문서 한자 0건 이모지 0건 가운뎃점 0건 전각 대시 0건
의존: 없음

### I02 이상 조항 규칙 저장 창구와 배선
상태: 통과
모드: 중량
범위: apps/web/app/api/rfp/rules/route.ts (신규), apps/web/components/rfp/RuleSettings.tsx, apps/web/lib/rfp/anomaly/rules.ts
감사 기준:
- 스위치를 켜고 끄면 rfp_anomaly_rules 에 남고 새로고침 뒤에도 그대로
- 저장 실패를 화면이 말함, 조용히 되돌아가지 않음
- 권한 확인이 창구에 있음, 화면만 가리지 않음
- 실패해도 화면이 켜진 척하지 않음 (낙관적 갱신을 되돌림)
- tsc 통과
의존: I01

### I03 알림 화면 신설
상태: 통과
모드: 경량
범위: apps/web/app/(rfp)/rfp/admin/NotificationSettings.tsx (신규), apps/web/app/(rfp)/rfp/admin/page.tsx
감사 기준:
- rfp_notifications 는 창구가 이미 있고 화면이 0개, 그 표를 읽어 보여 줌
- 화면에 적는 말은 용어집을 지남
- 읽기만 하는 화면이면 읽기만 한다고 화면이 말함, 못 바꾸는 것을 바꿀 수 있는 것처럼 두지 않음
- tsc 통과
의존: I02

### I04 저장 없는 설정 화면을 막는 가드
상태: 통과
모드: 경량
범위: apps/web/lib/policy/settings-save-guard.test.ts (신규), apps/web/package.json
감사 기준:
- 설정 화면이 값을 바꿀 수 있으면 저장 요청이 있어야 함, 없으면 실패
- 읽기 전용 화면은 등재로 면제하되 사유를 함께 적음
- 일부러 깨서 실패 확인
- 규칙이 도는 대상이 0개면 실패하는 단정
- 등재 후 총 테스트 수 증가 확인
의존: I03

## 종합 감사

실행한 명령과 결과 (2026-09-16)

- pnpm tsc --noEmit 통과, 오류 0건
- pnpm lint 통과, exit 0
- pnpm test 5585건 중 5581 통과, 실패 4는 전부 다른 세션이 편집 중인 미팅 끝내기와 digest 작업
- 내 가드 7종 36건은 전부 통과
- pnpm build 는 힙 부족으로 한 번 죽고 max-old-space-size 8192 로 다시 돌려 성공 (코드 문제 아님)

완료 정의 대조

- 화면에서 바꾼 설정이 새로고침 뒤에도 남음 (이상 조항 규칙이 rfp_anomaly_rules 에 저장됨)
- 저장 창구 없이 바꿀 수 있는 설정 화면 0개, 가드 settings-save-guard 가 잠금
- 설정값을 env 로 넣지 않음

전체 diff 검토

- 시작 커밋 aed226b7, 항목 커밋 4개 (v0.10.39 부터 v0.10.44)
- 범위 밖 변경 0건, 다른 세션의 미팅과 digest 와 nav 작업은 한 번도 안 실림
- diff 에 키 토큰 비밀번호 0건

발견 사항 (계획에 없던 것 셋)

- 설정 화면 넷 중 바꿀 수 있는 것은 하나뿐이었고 그 하나가 저장이 안 됐음
- 가드를 폴더로 잡으면 위젯 셋을 잘못 잡음, 설정 페이지가 그리는 것으로 정의해야 오탐이 0
- 내 버전 가드가 실제 어긋남을 잡음: 다른 세션 커밋이 bump 와 커밋 사이에 끼어 커밋은 v0.10.43 인데 package.json 은 0.10.42 였음

남은 것

- 아예 없는 화면 다섯 (문서 등급 편집, 조직과 구성원, 요금제, 수집 주기, 리포트 파생 필드)
- 공공데이터포털 서비스 상태 (8단계), 카탈로그와 능력별 프롬프트 (9단계)

## 변경 이력
- v0.1.0 (2026-09-16) 최초 작성 (ins_0007)
