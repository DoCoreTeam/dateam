# PLAN newAX: AI 부품 한 벌과 능력 대 부품 짝 가드
플랜 ID: P0006
플랜 버전: v0.1.2
상태: 완료
지시: ins_0007
목표 버전: v0.10.16
작성: 2026-09-14
시작 커밋: d367fd27

## 목표
- AI 결과를 그리는 공용 부품이 0개다, 서비스마다 자기 부품을 만들어 같은 값이 화면마다 다르게 보인다
- 부품 한 벌을 패키지에 두고, 능력마다 무엇을 함께 보여야 하는지를 가드로 강제한다
- 추출은 검수 없이 확정 못 하고, 생성은 미리보기 없이 저장 못 하고, 판정은 근거 없이 못 뜨고, 질의응답은 출처 없이 답 못 한다

## 범위 밖
- 자작 부품 26개를 실제로 이관하는 일 (기획서 9단계)
- 벤더 직접 호출 29곳을 관문으로 옮기는 일 (기획서 4단계)
- 카탈로그 신설
- 뼈대 생성 방식 (기획서 5절에서 채택 안 함으로 정함)

## 완료 정의
- pnpm tsc --noEmit, pnpm lint, pnpm test, pnpm build 통과
- 부품 여덟이 패키지에 있고 한글 0건, 라벨은 쓰는 쪽이 준다
- 능력 대 부품 짝이 가드로 강제되고 일부러 깨면 실패한다
- 부품이 실제 화면 한 곳에 붙어 동작한다

## 참조
- docs/2026-09-10-ai-common-layer/README.md 2절 세 층과 짝, 5절 자동 배치, 12절 놓치기 쉬운 것 1과 2와 12
- packages/ai-core/src/capability.ts 의 REQUIRED_PRESENTATION
- apps/web/components/ui/DateField.tsx 와 MoneyField.tsx (값의 종류가 부품을 정하는 선례)

## 항목

### I01 자작 부품을 세고 여덟이 무엇을 대신하는지 적는다
상태: 통과
모드: 경량
범위: docs/2026-09-10-ai-common-layer/step3-measure.md (신규)
감사 기준:
- AI 결과를 그리는 부품을 서비스별로 전수로 세고 파일 목록을 적음
- 부품 여덟(AI 값, 근거보기, 후보목록, 제안대조, 생성고지, 고친흔적, 물어보기, 진행상태)이 각각 어느 자작 부품을 대신하는지 표로
- 공용 부품 중 AI 결과를 그리는 것이 0개인 것을 다시 확인
- 날짜 칸과 금액 칸이 어떻게 값의 종류로 부품을 정하는지 읽고 같은 방식을 쓸 수 있는지 적음
- node --test 가 tsx 를 아예 못 읽는 것을 실측으로 확인하고(ERR_UNKNOWN_FILE_EXTENSION) 부품 패키지를 두 겹으로 나눌 근거로 적음
- 문서 한자 0건 이모지 0건 가운뎃점 0건 전각 대시 0건
의존: 없음

### I02 부품 패키지 자리를 만들고 라벨 주입 계약을 세운다
상태: 통과
모드: 경량
범위: packages/ai-react (신규), apps/web/package.json, apps/web/next.config.js, pnpm-workspace.yaml
감사 기준:
- 패키지가 React 를 peer 로 두고 앱이 쓰는 것과 같은 React 를 쓰는 것을 확인 (두 벌이면 훅이 죽음)
- 부품이 한글을 한 글자도 안 가짐, 라벨은 전부 props 로 받음
- 라벨을 안 주면 타입 검사에서 막힘, 기본 문구로 조용히 채우지 않음
- 부품 패키지를 두 겹으로 나눔: 무엇을 언제 보일지 정하는 규칙은 ts 에 두고 시험이 직접 읽음, tsx 는 그 결정을 그리기만 하는 얇은 층
- 규칙이 tsx 안에 있으면 시험이 못 보고 못 보는 규칙은 지켜지지 않는다는 것을 주석으로 남김
- 가장 작은 부품 하나로 tsc 와 test 와 build 셋 다 통과하는 것을 확인
의존: I01

### I03 AI 값 부품과 근거보기
상태: 통과
모드: 경량
범위: packages/ai-react/src/value.ts (신규), packages/ai-react/src/AiValue.tsx (신규), packages/ai-react/src/Evidence.tsx (신규), packages/ai-react/src/value.test.ts (신규), apps/web/package.json
감사 기준:
- AI 값 부품이 값과 확신과 출처를 함께 그림, 확신이 null 인 것과 0 인 것을 다르게 그림
- 근거보기가 원문 블록 번호와 글자 자리를 받아 그 자리를 짚음, 근거가 없으면 없다고 말함
- 값이 이전 판이면 그 사실을 그림 (2단계에서 만든 사다리 결과를 받음)
- node --test 로 통과, 등재 후 총 테스트 수 증가 확인
의존: I02

### I04 후보목록과 제안대조와 고친흔적
상태: 통과
모드: 경량
범위: packages/ai-react/src/review.ts (신규), packages/ai-react/src/CandidateList.tsx (신규), packages/ai-react/src/SuggestionDiff.tsx (신규), packages/ai-react/src/CorrectionTrail.tsx (신규), packages/ai-react/src/review.test.ts (신규), apps/web/package.json
감사 기준:
- 후보목록이 확정 전 상태를 그리고 사람이 고르기 전에는 확정으로 안 넘어감
- 제안대조가 지금 값과 제안 값을 나란히 보여 주고 받기와 버리기를 사람이 고름
- 고친흔적이 누가 언제 무엇을 무엇으로 바꿨는지 그림, 이전 값을 안 버림
- node --test 로 통과, 등재 확인
의존: I03

### I05 생성고지와 물어보기와 진행상태
상태: 통과
모드: 경량
범위: packages/ai-react/src/notice.ts (신규), packages/ai-react/src/GeneratedNotice.tsx (신규), packages/ai-react/src/AskAgain.tsx (신규), packages/ai-react/src/Progress.tsx (신규), packages/ai-react/src/notice.test.ts (신규), apps/web/package.json
감사 기준:
- 생성고지가 AI 가 만든 것임을 말함, 숨길 수 없음 (안 그리는 길이 없음)
- 물어보기가 같은 값에 다시 물어보는 자리를 줌
- 진행상태가 흘려보내는 중간값을 그림, 다 안 왔는데 확정으로 안 보임
- node --test 로 통과, 등재 확인
의존: I04

### I06 능력 대 부품 짝 가드
상태: 통과
모드: 경량
범위: apps/web/lib/policy/ai-pairing-guard.test.ts (신규), packages/ai-react/src/index.ts, apps/web/package.json
감사 기준:
- REQUIRED_PRESENTATION 표를 읽어 능력마다 필요한 부품이 실제로 패키지에 있는지 확인
- 추출 결과를 그리면서 후보목록을 안 쓰면 실패, 생성 결과에 미리보기가 없으면 실패, 판정에 근거가 없으면 실패, 질의응답에 출처가 없으면 실패
- 네 규칙을 각각 일부러 깨서 실패 확인
- 규칙이 도는 대상이 0개면 실패하는 단정
- 등재 후 총 테스트 수 증가 확인
의존: I05

### I07 실제 화면 한 곳에 붙인다
상태: 통과
모드: 경량
범위: apps/web/components/rfp 안 리포트 값 표시 경로, apps/web/lib/terms/ai.ts (신규), apps/web/lib/terms/index.ts
감사 기준:
- RFP 리포트에서 자작 둘이 빠지고 공용이 들어감: 고지 배지가 GeneratedNotice 로, 확신 판정이 공용 규칙으로
- 근거 목록은 일부러 안 바꿈, 지금 접혀 있어 화면 밀도를 지키는데 공용 목록으로 바꾸면 값 하나가 화면 반쪽을 먹음 (자작 이관은 9단계)
- 확신 문턱이 제품마다 세 값(RFP 0.6, 주간보고 0.5, 패키지 기본 0.7)인 것을 발견, 규칙만 합치고 숫자는 각 화면 몫으로 둠, 조용히 합치면 여러 화면이 불확실로 표시하는 값이 달라짐
- 화면에 적는 말은 전부 용어집을 지남, 부품에 한글이 안 들어감
- 그 화면의 기존 시험이 그대로 통과
- 실제 렌더 경로를 먼저 확인하고 고침 (중량 규정 실제 렌더 경로 우선)
- tsc 와 build 통과
의존: I06

## 종합 감사

실행한 명령과 결과 (2026-09-14)

- pnpm tsc --noEmit 통과, 오류 0건
- pnpm lint 통과, exit 0
- pnpm test 통과, 5537건 전부 통과, 실패 0
- NEXT_DIST_DIR=.next-check pnpm build 통과

완료 정의 대조

- 세 명령 전부 통과
- 부품 여덟이 packages/ai-react 에 있고 한글 0건, 라벨은 전부 쓰는 쪽이 줌
- 능력 대 부품 짝이 가드로 강제되고 셋 다 일부러 깨서 실패 확인
- 부품이 실제 화면 둘(RemedyPanel, RFP 리포트)에 붙어 빌드 청크까지 실림

전체 diff 검토

- 시작 커밋 d367fd27, 항목 커밋 7개 (v0.10.16 부터 v0.10.22)
- 범위 밖 변경 0건, 커밋마다 경로 지정
- diff 에 키 토큰 비밀번호 0건

발견 사항 (계획에 없던 것 넷)

- node --test 가 tsx 를 확장자 자체로 거부해 부품 패키지를 규칙 ts 와 그리기 tsx 두 겹으로 나눔
- report meta 의 aiNotice 를 그리는 화면이 0곳이었고 RemedyPanel 은 화면에 한글을 직접 박고 있었음
- 확신 문턱이 제품마다 세 값(0.6, 0.5, 0.7), 규칙만 합치고 숫자는 화면 몫으로 둠
- TS2802 을 다시 밟음, 앱 tsconfig 에 target 이 없어 ES5 로 떨어지는 것이 근원이고 이번에도 Array.from 으로 피함

남은 것

- 자작 부품 스물여덟을 실제로 이관하는 일은 9단계
- 근거 목록은 화면 밀도 때문에 이번에 안 바꿈
- 확신 문턱 숫자를 하나로 합칠지는 제품 결정이라 남겨 둠

## 변경 이력
- v0.1.0 (2026-09-14) 최초 작성 (ins_0007)
- v0.1.1 (2026-09-14) node --test 가 tsx 를 아예 못 읽어 부품 패키지를 규칙 ts 와 그리기 tsx 두 겹으로 나눔 (audit:I01)
- v0.1.2 (2026-09-14) 확신 문턱이 제품마다 세 값이라 규칙만 합치고 숫자는 화면 몫으로 둠, 근거 목록은 밀도 때문에 9단계로 미룸 (audit:I07)
