# PLAN newAX: AI 키 목록: 단추 이름과 유료 키 나중 쓰기
플랜 ID: P0034
플랜 버전: v0.1.4
상태: 완료
지시: ins_0044
목표 버전: v0.10.251
작성: 2026-09-20
시작 커밋: 85b6991d

## 목표
- 키를 더하는 단추가 무엇을 하는 단추인지 이름으로 말함 (지금은 「새 키」라 새 칸을 여는 단추로 읽힘)
- 유료 키를 표시하고, 무료 키가 다 마른 뒤에만 부름 (지금은 유료인지 물어보는 자리 자체가 없음)
- 화면에 보이는 줄 순서가 실제로 부르는 순서와 같음

## 범위 밖
- 유료 키의 비용을 원장에 따로 집계하는 일 (ai_llm_calls 는 이미 key_ref 를 적고 있고 집계는 다른 판)
- 공급자별 상한이나 예산 게이트 연동 (P0030 의 몫)
- META 칸 폐지 (마이그 264 머리주석대로 별도 판)
- 화면 밖 자동 판정 (키 문자열만 보고 유료인지 알 길이 없음, 사람이 표시함)

## 완료 정의
- pnpm tsc --noEmit, pnpm test, pnpm build 통과
- 사용자 노출 문자열은 전부 lib/terms 상수 (이 저장소의 말 SSOT, glossary 가드가 셈)
- 설정값은 env 추가 없이 DB 저장 + UI 관리
- 관리자 화면의 줄 순서와 lib/ai/key-pool 의 시도 순서가 같은 규칙을 씀

## 참조
- LOOP.md 7절 보안 기준 (S1 표 잠금, S2 창구 인증, S3 비밀 유출)
- supabase/migrations/264_ai_provider_keys.sql 머리주석 (이 표의 설계 근거와 잠금 판)
- apps/web/lib/terms/action.ts 의 createLabel 주석 (「추가」는 무엇을 추가하는지 밝혀야 함)
- apps/web/lib/policy/ai-key-pool.test.ts (원문 키가 흐르는 길 가드)

## 항목

### I01 키를 더하는 단추 이름을 「추가」로
상태: 통과
모드: 경량
범위: apps/web/lib/terms/ai-key.ts, apps/web/lib/terms/terms.test.ts
감사 기준:
- pnpm test 의 terms 시험이 AI_KEY.create === '추가' 를 단정하고 통과
- 맨 「추가」를 쓰는 예외와 그 사유가 lib/terms/ai-key.ts 에 적혀 있음 (카드 제목과 폼 라벨이 무엇을 더하는지 이미 말함, MEETING_CAPTURE_LABEL 과 같은 방식)
- grep -rn "새 키" apps/web/app apps/web/components 결과에 단추 라벨이 없음
- node --test lib/ui/glossary.test.ts 통과 (금지어 baseline 이 오르지 않음)
의존: 없음

### I02 마이그 269 is_paid 칼럼
상태: 통과
모드: 중량
범위: supabase/migrations/269_ai_provider_keys_paid.sql (신규)
감사 기준:
- 보안 S1: CREATE TABLE 이 없고 ADD COLUMN IF NOT EXISTS 만 있음, 264 에서 켠 RLS 와 GRANT 판이 그대로임을 SQL 본문으로 확인
- 되돌리기 한 줄이 머리주석에 있음 (drop column if exists is_paid)
- 고르는 인덱스가 새 순서를 탐 (provider, is_paid, priority, id)
- psql 로 적용 후 \d ai_provider_keys 에 is_paid boolean not null default false 가 보임
의존: 없음

### I03 고르는 규칙: 무료 먼저 유료 나중
상태: 통과
모드: 경량
범위: apps/web/lib/ai/key-pool.ts, apps/web/lib/ai/key-pool.test.ts, apps/web/lib/ai/gemini-call.test.ts, apps/web/lib/rfp/ai/host-caller.test.ts, apps/web/lib/ai/key-store-core.ts (그 형을 만드는 두 자리만)
감사 기준:
- 무료 키가 하나라도 쓸 수 있으면 유료 키가 앞에 오지 않음을 단정하는 시험이 통과
- 무료가 전부 쉬는 중이면 유료가 나옴을 단정하는 시험이 통과
- 같은 등급 안에서는 priority 가 낮은 것이 먼저임이 유지됨 (기존 시험 그대로 통과)
- 전부 쉬는 중일 때 가장 빨리 풀리는 하나를 주는 규칙이 안 깨짐 (기존 시험 그대로 통과)
- KeyPoolEntry 를 손으로 만드는 시험 도우미 둘이 새 칸을 빠뜨리지 않음 (tsconfig 가 *.test.ts 를 검사 밖에 두어 tsc 가 안 잡는 자리)
- pnpm tsc --noEmit 통과 (KeyPoolEntry 를 만드는 rowToEntry 와 metaEntry 가 새 칸을 채움)
의존: 없음

### I04 저장소와 창구가 유료 표시를 읽고 씀
상태: 통과
모드: 중량
범위: apps/web/lib/ai/key-store-core.ts, apps/web/lib/ai/key-store.ts, apps/web/app/admin/settings/actions.ts, apps/web/lib/ai/key-store.test.ts
감사 기준:
- 보안 S2: 새 서버액션 setProviderKeyPaid 가 requireAdmin 을 먼저 부름을 코드로 확인, 안 부르는 판으로 일부러 깨 보고 실패를 봄
- 보안 S3: 화면으로 나가는 KeyView 에 api_key 가 없음 (기존 가드 「원문 키가 브라우저로 가지 않는다」 통과)
- 유료 표시를 바꿔도 priority 는 안 건드리고, 표시를 되돌리면 원래 순서로 돌아옴을 단정하는 시험이 통과
- 순서 단추가 등급 경계를 넘지 못함을 단정하는 시험이 통과 (규칙 모듈이 빈 목록을 돌려줌)
- listKeys 가 돌려주는 순서가 key-pool 의 시도 순서와 같음을 단정하는 시험이 통과
의존: I02, I03

### I05 화면: 유료 표시와 순서
상태: 통과
모드: 경량
범위: apps/web/app/admin/settings/AiProviderCard.tsx, apps/web/lib/terms/ai-key.ts
감사 기준:
- 키 추가 폼에 유료 여부를 고르는 자리가 있고, 줄마다 유료 배지와 전환 단추가 있음
- 순서 단추가 등급 경계를 넘지 않음 (무료 마지막 줄의 아래로, 유료 첫 줄의 위로가 비활성)
- 화면에 한글 문자열을 직접 적지 않음 (전부 AI_KEY 상수)
- 안내문이 유료 규칙을 말함 (무료가 다 마른 뒤 유료를 부른다)
의존: I04

### I06 가드: 유료 나중 규칙이 흩어지지 않게
상태: 통과
모드: 경량
범위: apps/web/lib/policy/ai-key-pool.test.ts
감사 기준:
- 유료 판정을 key-pool 밖에서 다시 하는 자리가 0 임을 세는 단정이 있고 통과
- 마이그 269 가 표를 새로 만들지 않고 칸만 더함을 세는 단정이 있고 통과
- 가드를 일부러 깨뜨려 실패를 확인하고 그 사실을 pass 기록에 적음
의존: I03, I04

### I06a reorderPriorities 옛 호출 자리 하나
상태: 통과
모드: 경량
범위: apps/web/lib/ai/provider-keys.test.ts
감사 기준:
- pnpm test 실패 0 (지금 not ok 2건, 둘 다 옛 서명으로 부르는 자리)
- 같은 파일의 「끝에서 더 밀면」 단정이 우연히 통과하던 것을 고침 (빈 목록을 기대하는데 서명이 안 맞아 빈 목록이 나와 초록이었음)
의존: I04

## 종합 감사

전 항목 7/7 통과 (I01 I02 I03 I04 I05 I06 I06a)

### 1 검사 넷

| 명령 | 결과 |
|---|---|
| pnpm tsc --noEmit | 통과, 출력 없음 |
| pnpm lint | 오류 0, 경고 26 (전부 이전부터 있던 것, 내 파일 0) |
| pnpm test | 6305/6305 통과, 실패 0 |
| NEXT_DIST_DIR=.next-paid pnpm build | exit 0, 전 라우트 생성 |

빌드는 격리 dist 로 돌렸고(옆 세션 dev 서버의 .next 를 안 덮게), 빌드가 고친
tsconfig.json 과 next-env.d.ts 는 되돌리고 .next-paid 는 지웠음

### 2 완료 정의 대조

- tsc test build 통과: 위 표
- 사용자 노출 문자열이 전부 lib/terms 상수: 내가 더한 줄에 한글 리터럴 0 (남은 한글은 주석뿐), glossary 가드 통과하고 금지어 baseline 변동 없음
- env 추가 없이 DB 저장 + UI 관리: 새 설정값은 ai_provider_keys.is_paid 한 칸이고 관리자 화면에서 켜고 끔, .env 계열 파일 변경 0
- 화면 순서와 시도 순서가 같은 규칙: 저장소 두 질의가 (is_paid, priority, id) 한 문장을 쓰고, 가드가 그 순서를 셈

### 3 전체 diff

내 커밋 일곱: 93a0f7cb 3248ad97 5267f2a7 f64ce770 a5a3bbd7 a3cfeffe 469d881c
(v0.10.251 ~ v0.10.258, 그 사이 v0.10.257 은 옆 세션 몫)

- 범위 밖 파일 0. 일곱 커밋이 건드린 것은 플랜 범위 파일과 버전 파일 다섯뿐
- 비밀 0. 다섯 공급자 키 모양(AIza sk-ant- sk- gsk_ xai-)과 password secret service_role_key 로 훑어 0건
- 하드코딩 문자열 0. 화면 말은 전부 AI_KEY 상수

### 3a 보안 재측정 (7절 다섯 줄, docs/policy/security-count.sql)

| 세는 것 | 값 |
|---|---|
| rls_off_tables | 0 |
| anon_write_tables | 0 |
| public_using_true_policies | 0 |
| unpinned_secdef_functions | 0 |
| anon_readable_secdef_views | 0 |

마이그 269 는 표를 만들지 않고 칸만 더해 264 의 잠금 판이 그대로임
psql \d 실측: Policies (forced row security enabled): (none), anon 과 authenticated 권한 0행

### 4 항목 대 결과

| 항목 | 남은 것 |
|---|---|
| I01 | AI_KEY.create === '추가', 예외 사유 주석, terms 단정 |
| I02 | supabase/migrations/269_ai_provider_keys_paid.sql 적용됨, is_paid 칼럼과 인덱스 실재 |
| I03 | key-pool 의 tier 비교자와 orderForView, 단정 여섯 |
| I04 | KeyView.isPaid, 등급 경계를 막는 reorderPriorities, setKeyPaid, setProviderKeyPaid |
| I05 | 추가 폼 체크상자, 유료 배지, 전환 단추, 경계에서 비활성인 순서 단추 |
| I06 | 정책 가드 네 줄 (흩어짐 정렬 읽는순서 마이그) |
| I06a | 옛 서명으로 부르던 시험 셋 |

### 5 운영 데이터 실측

실브라우저 확인은 못 했음 (Chrome 확장이 이 세션에 안 붙어 있음)
대신 운영 표로 같은 길을 쟀음

- 되돌리는 트랜잭션 안에서 첫 줄을 유료로 표시 → 그 줄이 맨 뒤로 감 → ROLLBACK 후 원래 순서 복귀
- gemini 키 넷 중 label 이 「유료키」인 줄이 이미 있었고 표시는 무료였음 (사람이 손으로 맨 뒤에 둬서 버티던 자리)
  그 줄만 is_paid = true 로 바꿈, 되돌리려면 그 줄의 전환 단추를 한 번 누르면 됨

### 6 발견

- tsconfig 가 `*.test.ts` 를 검사 밖에 둬서 형을 바꿔도 시험 호출 자리를 tsc 가 못 잡음
  이번 판에서만 네 자리가 그렇게 걸렸고 그중 하나(provider-keys.test.ts 의 「끝에서 더 밀면」)는
  빈 목록을 기대하는 단정이라 서명이 안 맞아도 초록이었음. 이 플랜에서 고칠 일은 아니고 별도 판감
- 마이그 266 가드의 `key_ref` 단정에 낱말 끝이 없어 `key_ref_x` 로 바뀌어도 통과하던 구멍을 같이 막음
- 업데이트 내역(entries.ts)은 안 씀. 이 판은 전부 관리자 설정 화면이고 entries.ts 규율이 어드민 전용을 제외함

## 변경 이력
- v0.1.0 (2026-09-20) 최초 작성 (ins_0044, 그리고 같은 턴에 온 유료 키 지시)
- v0.1.1 (2026-09-20) I01 단추 이름을 「추가」로 (iv_0080, 사용자 지시)
- v0.1.1 (2026-09-20) KeyPoolEntry 에 isPaid 를 더하자 시험 도우미 둘이 그 칸 없이 그 형을 만들고 있었다. tsconfig 가 *.test.ts 를 검사 밖에 두어 tsc 가 안 잡는다, 그대로 두면 형만 맞는 척하는 자리가 남는다 (audit:I03)
- v0.1.2 (2026-09-20) 형을 바꾸면 그 형을 만드는 자리가 같은 판에서 컴파일돼야 한다. key-store-core 의 rowToEntry 와 metaEntry 둘만 I03 으로 당기고, 표에 쓰는 길과 화면 순서는 I04 에 그대로 둔다 (audit:I03)
- v0.1.3 (2026-09-20) priority 를 다시 매기려던 기준을 버렸다. 등급이 순서를 이기므로 priority 를 그대로 두면 유료 표시를 되돌렸을 때 원래 순서가 그대로 돌아온다, 다시 매기면 그 성질이 사라지고 쓰기도 는다. 대신 순서 단추가 등급 경계를 못 넘는다는 기준을 넣었다 (audit:I04)
- v0.1.4 (2026-09-20) 종합 감사에서 pnpm test 실패 2건을 잡았다. reorderPriorities 서명을 바꿨는데 provider-keys.test.ts 가 옛 모양(문자열 배열)으로 부르고 있었고, tsconfig 가 *.test.ts 를 검사 밖에 둬서 tsc 가 못 봤다. 게다가 그 파일의 「끝에서 더 밀면」 단정은 빈 목록을 기대하는데 서명 불일치로도 빈 목록이 나와 거짓 초록이었다 (audit:I06)
