# PLAN newAX: 개발자센터 AI 절을 읽고 붙일 수 있는 문서로
플랜 ID: P0027
플랜 버전: v0.1.1
상태: 완료
지시: ins_0028
목표 버전: v0.10.163
작성: 2026-09-19
시작 커밋: 98fb5c76

## 목표
- AI 절의 말투와 제목을 화면 나머지(시작하기·엔드포인트·오류)와 한 벌로 맞춘다
- 수출 이름만 나열하던 것을 인자·반환·오류·예시가 있는 참조 문서로 만든다
- 코드에는 있는데 문서에 없던 모델 정책(공급자 다섯, 후보 상한, 429 처리, 능력 판정)을 절로 세운다

## 범위 밖
- 새 엔드포인트·새 패키지 수출 추가 (문서만 고친다)
- packages/*/src, apps/web/lib/ai-chat/model-chain.ts, apps/web/lib/crm/ai/* (읽기만 하고 고치지 않는다)
- 시작하기·엔드포인트·오류·직접 실행 절의 문구 (이미 제품 문서 말투라 손대지 않는다)
- 영어판 (이 화면은 한국어 한 벌이다)

## 완료 정의
- pnpm tsc --noEmit, pnpm lint, pnpm test, pnpm build 통과
- AI 절 네 항목 전부에 제목과 한 줄 설명이 뜬다 (화면 나머지와 같은 PageHeader)
- AI 절 화면 문구에 「관문」「등록부」「형 검사」「판 번호」「패키지 넷」「능력 여덟」류 지어낸 말이 0건
- AI 절에 만든 사람 회고(「왜 따로 뺐나」「실측 …였다」「지어냈다」)가 0건
- 주요 수출마다 호출 모양과 반환 모양을 화면에서 볼 수 있다
- 모델 정책 절에서 공급자 목록·후보 상한·429 처리·능력 판정을 읽을 수 있고, 그 숫자가 코드와 같다
- 새 내용도 가드가 센다 (pnpm test 에서 ai-layer-docs-guard 통과, 일부러 깨서 확인)

## 참조
- apps/web/lib/api-docs/ai-layer.ts — AI 절 문구 SSOT, 화면은 그리기만 한다
- apps/web/lib/policy/ai-layer-docs-guard.test.ts — 문서가 드는 이름을 실제 수출과 대조하는 가드
- apps/web/app/develop/page.tsx StartSection·ErrorsSection — 이 화면이 이미 쓰는 제품 문서 말투의 본보기
- packages/{ai-core,ai-gateway,ai-providers,ai-react}/src — 시그니처의 출처
- apps/web/lib/ai-chat/model-chain.ts, packages/ai-providers/src/vendor.ts — 모델 정책의 출처

## 항목

### I01 말투와 제목을 화면 나머지와 맞춘다
상태: 통과
모드: 경량
범위:
- apps/web/lib/api-docs/ai-layer.ts
- apps/web/app/develop/AiLayerSection.tsx
감사 기준:
- 네 항목 전부에 PageHeader 가 붙는다: grep -c "PageHeader" apps/web/app/develop/AiLayerSection.tsx 가 2 이상이고 AI_DOC_NAV 항목마다 title·description 이 ai-layer.ts 에서 온다
- 소개 절이 읽는 사람 기준으로 다시 쓰인다: 「무엇을 해 주나 · 누가 읽는 절인가 · 무엇을 안 해 주나」, 만든 사람 회고 절(「왜 따로 뺐나」)은 사라진다
- 지어낸 번역어가 표준어로 바뀐다: 관문→게이트웨이, 등록부→공급자 목록, 판 번호→계약 버전, 형 검사→타입 검사, 사슬→모델 후보, 패키지 넷→패키지, 능력 여덟→능력
- 개수 제목이 사라진다: grep -n "패키지 넷\|능력 여덟\|일곱\|넷을 다" apps/web/lib/api-docs/ai-layer.ts apps/web/app/develop/AiLayerSection.tsx 가 0건
- 종결이 화면 나머지와 같은 「~합니다」다: AiLayerSection.tsx 에 직접 박힌 한글 문구가 0건이고 전부 ai-layer.ts 에서 온다
- pnpm tsc --noEmit 통과
의존: 없음

### I02 모델 정책 절을 세운다
상태: 통과
모드: 경량
범위:
- apps/web/lib/api-docs/ai-layer.ts
- apps/web/app/develop/AiLayerSection.tsx
감사 기준:
- 왼쪽 목록에 항목이 하나 늘고 눌렀을 때 그 절이 뜬다: AI_DOC_NAV 에 ai-policy 가 있고 AiLayerSection 의 switch 가 그 가지를 든다
- 공급자 표가 packages/ai-providers/src/vendor.ts 와 같다: id·키 앞자리·vision·tools 다섯 줄
- 후보 숫자가 코드와 같다: 전체 상한 6, 공급자당 2 (model-chain.ts 의 MAX_CHAIN_CANDIDATES · MAX_PER_PROVIDER)
- 실패 처리가 적힌다: scope 가 provider 면 그 공급자를 통째로 빼고, model 이면 그 모델만 뺀다
- 능력을 못 채우는 공급자가 후보에서 빠진다는 것과, 후보가 0개면 조용히 끝내지 않는다는 것이 적힌다
- 갈아탔을 때 사용자에게 한 줄이 보인다는 것과 그 문장이 formatFallbackNotice 한 곳에서 온다는 것이 적힌다
- pnpm tsc --noEmit 통과
의존: I01

### I03 패키지 절을 참조 문서로 만든다
상태: 통과
모드: 경량
범위:
- apps/web/lib/api-docs/ai-layer.ts
- apps/web/app/develop/AiLayerSection.tsx
감사 기준:
- 주요 수출마다 호출 모양이 화면에 뜬다: callWithFallback · maskPii · pickModels · newAiValue · applyCorrection · climb · canRead · confidenceView · missingLabels 아홉에 시그니처 한 줄과 설명이 붙는다
- 그 시그니처가 실제 소스와 같다 (packages/*/src 에서 확인하고 근거를 notes 에 적는다)
- 던지는 오류가 표로 뜬다: TransferBlockedError 외 실제로 던지는 것만, 언제 나는지와 무엇을 하면 되는지
- 수출 칩 나열만 하던 자리가 사라진다
- pnpm tsc --noEmit 통과
의존: I01

### I04 결과 계약 절에 실제 타입과 상태 전이를 넣는다
상태: 통과
모드: 경량
범위:
- apps/web/lib/api-docs/ai-layer.ts
- apps/web/app/develop/AiLayerSection.tsx
감사 기준:
- AiValue 타입이 코드 블록으로 뜨고 packages/ai-core/src 의 정의와 필드가 같다
- 상태 넷(streaming·candidate·confirmed·corrected)과 허용 전이가 표로 뜨고 canTransition 과 같다
- 능력 표의 이름이 사람이 읽는 말로 바뀐다 (뽑기→추출 등), key 는 계약 그대로 둔다
- 한 화면을 처음부터 끝까지 만드는 예시 하나가 붙는다 (부르기 → 저장 → 그리기)
- pnpm tsc --noEmit 통과
의존: I01

### I05 새 내용도 가드가 센다
상태: 통과
모드: 경량
범위:
- apps/web/lib/policy/ai-layer-docs-guard.test.ts
감사 기준:
- 시그니처에 적은 함수 이름이 실제 소스에 있는지 센다 (없는 이름을 적으면 실패)
- 공급자 표가 vendor.ts 의 다섯과 같은지 센다 (id·키 앞자리·vision·tools)
- 후보 숫자가 model-chain.ts 의 상수와 같은지 센다
- 지어낸 말 금지 목록을 센다 (관문·등록부·패키지 넷·능력 여덟·형 검사·판 번호)
- AI_DOC_NAV 항목마다 제목과 설명이 있는지 센다
- 일부러 깨서 각 단정이 실제로 실패하는 것을 확인한다 (근거를 notes 에 적는다)
- pnpm test 통과, ai-layer-docs-guard 는 이미 등재돼 있어 package.json 을 건드리지 않는다
의존: I02, I03, I04

### I06 브라우저가 새 화면을 본다
상태: 통과
모드: 경량
범위:
- apps/web/e2e/develop-ai-layer.spec.ts
감사 기준:
- 명세가 옛 이름을 안 든다: grep -n "패키지 넷\|한 줄로\|일곱" apps/web/e2e/develop-ai-layer.spec.ts 가 0건
- 다섯 항목을 눌러 각각 제목이 바뀌는 것을 본다 (PageHeader 의 h1 이 AI_DOC_NAV 의 title 과 같다)
- 모델 정책 절에서 공급자 다섯과 상한 숫자와 실패 갈래 셋이 실제로 그려진다
- 패키지 절에서 시그니처 한 줄과 오류 표가 실제로 그려진다
- 결과 계약 절에서 타입 블록과 상태 전이 표가 실제로 그려진다
- 실제로 돌려서 통과한다 (pnpm exec playwright test apps/web/e2e/develop-ai-layer.spec.ts, 근거를 notes 에 적는다)
의존: I05

## 종합 감사
2026-09-19, 시작 커밋 98fb5c76 (v0.10.162) 부터 ca10aaa9 (v0.10.168) 까지

### 명령
- pnpm tsc --noEmit (apps/web): exit 0, error TS 0건
- pnpm lint: exit 0, 이번에 바꾼 네 파일 지적 0건
- pnpm test: 5887/5887 통과, fail 0 (그중 ai-layer-docs-guard 15건)
- next build (NEXT_DIST_DIR=.next-verify, dev 서버를 안 덮으려고 따로 냄): Compiled successfully, 정적 318/318, exit 0

### 완료 정의 대조
- AI 절 항목마다 제목과 한 줄 설명: 다섯 항목 전부, 빈 것 0건, PageHeader 로 화면 나머지와 같은 자리
- 지어낸 말 0건: 화면에 나가는 값 전체를 훑어 관문·등록부·패키지 넷·능력 여덟·형 검사·판 번호·사슬 0건
- 만든 사람 회고 0건: 왜 따로 뺐나·실측·지어냈·664커밋·스물다섯 0건
- 주요 수출마다 호출 모양과 반환 모양: 아홉, 정규화 대조로 실제 선언과 문자 단위 일치
- 모델 정책: 공급자 다섯(vendor.ts 파생), 상한 6·2(model-chain.ts 상수), 실패 갈래 셋, 능력 판정, 후보 0개 처리, 갈아탄 알림
- 가드: 단정 여덟을 새로 더했고 여덟 전부 일부러 깨서 실패하는 것을 확인

### 전체 diff
- 9파일 1005추가 163삭제, 그중 다섯은 버전 파일(package.json 둘, 정책 셋)
- 범위 밖 변경 없음, 비밀 0건(걸린 셋은 전부 반환 모양을 적은 문서 문자열: token·field_secret)

### 항목 대 결과
- I01 59c48545 v0.10.163 / I02 dd8cc2d3 v0.10.164 / I03 14a04ab2 v0.10.165
- I04 20389534 v0.10.166 / I05 7045d02e v0.10.167 / I06 ca10aaa9 v0.10.168
- 항목마다 범위에 적은 파일이 실제로 바뀜, I04·I05 의 가드 파일은 사유를 notes 에 적음

### 브라우저
- pnpm exec playwright test apps/web/e2e/develop-ai-layer.spec.ts: 6 passed, exit 0, 실제 로그인 세션
- 1440 폭·390 폭 스크린샷으로 표가 가로로 안 넘치고 좁은 화면에서 카드로 바뀌는 것을 눈으로 확인

### 발행
- 사용자향 업데이트 내역(entries.ts) 블록은 쓰지 않음, 이 화면은 개발자가 붙일 때 읽는 참조 문서라
  entries.ts 의 판정 기준(로그인한 일반 사용자가 화면에서 직접 체감하나)에 「아니오」이기 때문

### 남은 것 (이번 범위 밖, 고치지 않음)
- packages/ai-providers/src/pick.ts: internalOnly 로 빠진 모델의 이유가 'doc_class' 로 달린다,
  ExclusionReason 에 그 갈래가 없어서인데 화면이 「왜 이 모델이 없나」에 틀린 답을 하게 된다
- packages/ai-core/src/contract.ts 머리주석의 「The seven parts」는 실제 여덟 자리와 다르다

## 변경 이력
- v0.1.0 (2026-09-19) 최초 작성 (ins_0028)
- v0.1.1 (2026-09-19) I06 추가 — 종합 감사에서 옛 E2E 명세가 바뀐 이름을 그대로 들고 있는 것을 발견
- v0.1.1 (2026-09-19) 종합 감사에서 apps/web/e2e/develop-ai-layer.spec.ts 가 「패키지 넷」「한 줄로」「일곱」 같은 바뀐 이름을 그대로 들고 있는 것을 발견, 두면 돌리는 즉시 실패하므로 I06 으로 갱신하고 새 절까지 실제 브라우저로 본다 (audit:I05)
