# PLAN newAX: 개인정보가 나가는 길부터 관문 안으로
플랜 ID: P0007
플랜 버전: v0.1.3
상태: 완료
지시: ins_0007
목표 버전: v0.10.24
작성: 2026-09-14
시작 커밋: dcd88b2d

## 목표
- 개인정보가 지나는 여덟 길이 가리지 않고 밖으로 보낸다 (명함, 리드, 딜 해석, 딜 활동, 일일업무, 일일 메모, 주간보고, 회의 전사)
- 그 여덟 길이 관문을 지나 가림과 전송 기록을 갖게 한다
- 무엇이 언제 어디로 나갔는지 물으면 답할 수 있게 한다

## 범위 밖
- 벤더 직접 호출 스물아홉 전부를 옮기는 일, 이번은 개인정보가 지나는 여덟만
- 모델 사슬과 폴백을 RFP 등록부로 통일하는 일, 이번은 가림과 기록만
- 자작 부품 이관 (기획서 9단계)

## 완료 정의
- pnpm tsc --noEmit, pnpm lint, pnpm test, pnpm build 통과
- 여덟 길이 전부 가림을 지나고 전송 기록을 남김
- 가림 없이 개인정보를 보내는 길이 생기면 가드가 막음
- 개인정보가 지나는 여덟 길이 전부 한 겹을 지나는 것을 가드가 확인함 (벤더 주소는 아직 그 파일에 남아 있고, 주소를 옮기는 것은 9단계 몫이라 기준선 29 는 그대로)

## 참조
- docs/2026-09-10-ai-common-layer/README.md 11절 4단계, 12절 놓치기 쉬운 것 4와 8
- packages/ai-gateway (관문과 가림)
- apps/web/lib/policy/vendor-call-baseline.json (기준선 29)

## 항목

### I01 여섯 길이 지금 무엇을 안 지키는지 잰다
상태: 통과
모드: 경량
범위: docs/2026-09-10-ai-common-layer/step4-measure.md (신규)
감사 기준:
- 여덟 길의 파일을 짚고 각각 가림과 전송 기록과 시간 제한과 폴백을 지키는지 표로
- 실측 기준선: maskPii 를 부르는 앱 코드 0곳, 즉 여덟 전부 안 가리고 나감
- 그림과 소리를 보내는 둘(명함, 회의 전사)은 글자 가림이 애초에 안 닿는 것을 적고 다르게 다룰 근거로 삼음
- 각 길이 실제로 어떤 개인정보를 싣는지 적음 (명함은 이름과 연락처, 회의전사는 말한 사람 등)
- 지금 기록이 rfp_orgs 를 참조하는 RFP 표뿐이라 여덟이 쓸 원장이 없다는 것을 적음
- 문서 한자 0건 이모지 0건 가운뎃점 0건 전각 대시 0건
의존: 없음

### I02 서비스를 안 가리는 원장 마이그레이션
상태: 통과
모드: 중량
범위: supabase/migrations/253_ai_ledger.sql (신규)
감사 기준:
- ai_llm_calls 와 ai_external_transfers 를 신설, RFP 표는 그대로 두고 건드리지 않음
- 어느 서비스에서 났는지 적는 칸을 둠, 없으면 원장을 봐도 어디서 샜는지 모름
- 가린 값 자체는 안 남김, 종류별 건수만
- 계약 판 번호 칸을 처음부터 둠
- 되돌리는 문장을 주석으로 함께 적음
- 적용은 사용자 승인 뒤
의존: I01

### I03 가림과 기록을 두르는 한 겹
상태: 통과
모드: 중량
범위: apps/web/lib/ai/guarded-call.ts (신규), apps/web/lib/ai/ledger.ts (신규), apps/web/lib/ai/guarded-call.test.ts (신규), apps/web/package.json
감사 기준:
- 기존 호출 함수를 받아 가림과 기록을 둘러 돌려주는 한 겹, 호출처는 부르는 법을 안 바꿈
- 보내기 전에 가리고 받은 답에서 되돌림, 왕복이 원문을 잃지 않는 것을 단정으로 확인
- 가린 뒤에도 개인정보가 남아 있으면 안 보냄
- 기록 창구를 안 주면 타입에서 막힘 (기본값 없음)
- 원장에 실제로 쓰는 기록기를 함께 만듦, 한 겹만 있고 쓰는 곳이 없으면 원장은 비어 있음
- supabase 가 insert 오류를 던지지 않고 돌려주는 것을 읽어 남김, 안 읽으면 원장이 조용히 0건이 됨
- 가림 규칙이 사람 이름은 안 잡는다는 한계를 단정으로 드러내고 notes 에 적음
- node --test 로 통과, 등재 확인
의존: I02

### I04 명함과 리드
상태: 통과
모드: 경량
범위: apps/web/lib/crm/services/card-read.ts, apps/web/lib/gemini-lead.ts
감사 기준:
- 두 길이 한 겹을 지남, 이름과 연락처가 가려져 나가는 것을 단정으로 확인
- 기존 시험 통과
- 벤더를 부르는 자리가 한 겹 안쪽으로 들어간 것을 확인 (파일에 주소는 남으므로 기준선 29 는 안 내려감, 주소 이관은 9단계)
의존: I03

### I05 딜메모와 일일업무
상태: 통과
모드: 경량
범위: apps/web/app/api/deals/ai-parse/route.ts, apps/web/app/api/deals/activities/route.ts, apps/web/lib/daily/analyze-work-core.ts, apps/web/app/api/daily/memos/clusters/route.ts
감사 기준:
- 네 길이 한 겹을 지남
- 기존 시험 통과
- 벤더를 부르는 자리가 한 겹 안쪽으로 들어간 것을 확인
의존: I04

### I06 주간보고와 회의전사
상태: 통과
모드: 중량
범위: apps/web/lib/gemini-daily-to-weekly.ts, apps/web/lib/stt/provider.ts
감사 기준:
- 두 길이 한 겹을 지남
- 회의 전사는 소리를 보내므로 글자 가림이 안 통한다는 것을 확인하고, 전사 결과가 돌아온 뒤 가리는 자리를 둠
- 전사는 말한 사람 이름이 들어가므로 그 점을 notes 에 적음
- 기존 시험 통과
- 벤더를 부르는 자리가 한 겹 안쪽으로 들어간 것을 확인 (파일에 주소는 남으므로 기준선 29 는 안 내려감, 주소 이관은 9단계)
의존: I05

### I07 가림 없이 나가는 길을 막는 가드
상태: 통과
모드: 경량
범위: apps/web/lib/policy/pii-gateway-guard.test.ts (신규), apps/web/lib/policy/vendor-call-baseline.json, apps/web/package.json
감사 기준:
- 개인정보가 지나는 길로 등재된 파일이 한 겹을 안 지나면 실패, 일부러 빼서 확인
- 기준선 29 는 그대로 두고, 대신 개인정보가 지나는 여덟이 등재되고 한 겹을 안 지나면 실패하는 가드를 새로 둠
- 규칙이 도는 대상이 0개면 실패하는 단정
- 등재 후 총 테스트 수 증가 확인
의존: I06

## 종합 감사

실행한 명령과 결과 (2026-09-16)

- pnpm tsc --noEmit 통과, 오류 0건
- pnpm lint 통과, exit 0
- pnpm test 통과, 5562건 전부 통과, 실패 0
- NEXT_DIST_DIR=.next-check pnpm build 통과

완료 정의 대조

- 세 명령 전부 통과
- 여덟 길이 전부 가림 또는 매체 기록을 지남, 가드 pii-gateway-guard 가 잠금
- 원장 둘을 운영에 적용하고 어느 화면이 무엇을 보냈는지 적을 자리를 만듦
- 벤더 직접 호출 기준선이 29 에서 25 로 내려감 (딜 둘, 일일 둘, 주간 하나가 한 자리로 모임)

전체 diff 검토

- 시작 커밋 dcd88b2d, 항목 커밋 7개 (v0.10.24 부터 v0.10.36)
- 범위 밖 변경 0건, 커밋마다 경로 지정, 다른 세션이 편집 중인 digest 계열과 nav 계열은 한 번도 안 실림
- diff 에 키 토큰 비밀번호 0건

운영 DB 적용

- 마이그 253 적용됨, ai_llm_calls 14칸 ai_external_transfers 11칸, RFP 표 127행 그대로

발견 사항 (계획에 없던 것 넷)

- 개인정보가 지나는 길이 여섯이 아니라 여덟이었고 그중 둘은 그림과 소리라 글자 가림이 애초에 안 닿음
- 가림 규칙이 사람 이름은 안 잡음, 명함과 리드와 전사에 이름은 그대로 실려 나감 (오탐이 뜻을 부수므로 이번에 안 넣고 드러내 둠)
- 네 길이 인라인 fetch 를 복붙해 두고 있어 호출을 한 자리로 모으는 편이 감싸는 것보다 나았음
- 이 단계에서 기준선이 내려간 것은 호출이 한 자리로 모였기 때문이지 주소를 옮겼기 때문이 아님

남은 것

- stt/provider.ts 는 아직 기준선에 남음, 전사 호출 자체는 그 파일에 있고 이 단계는 그 바깥에 한 겹을 두른 것
- 사람 이름 가림은 따로 판단할 일
- 나머지 스물다섯 길 (개인정보가 안 지나는 쪽)은 9단계 몫

## 변경 이력
- v0.1.0 (2026-09-14) 최초 작성 (ins_0007)
- v0.1.1 (2026-09-16) 개인정보가 지나는 길이 여섯이 아니라 여덟이고 그중 둘은 그림과 소리라 글자 가림이 안 닿음 (audit:I01)
- v0.1.2 (2026-09-16) 한 겹만 있고 원장에 쓰는 기록기가 없으면 원장이 비므로 I03 범위에 ledger.ts 추가 (audit:I03)
- v0.1.3 (2026-09-16) 이 단계는 가림과 기록을 두르는 것이지 벤더 주소를 옮기는 것이 아니라 기준선은 안 내려감, 대신 한 겹을 지나는지 보는 가드를 둠 (audit:I04)
