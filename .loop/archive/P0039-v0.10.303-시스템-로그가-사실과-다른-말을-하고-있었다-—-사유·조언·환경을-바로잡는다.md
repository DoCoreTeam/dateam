# PLAN newAX: 시스템 로그가 사실과 다른 말을 하고 있었다 — 사유·조언·환경을 바로잡는다
플랜 ID: P0039
플랜 버전: v0.1.4
상태: 완료
지시: ins_0049
목표 버전: v0.10.303
작성: 2026-09-20
시작 커밋: 659a2866

## 목표
- 시스템 로그 아홉 줄을 원문과 대조한 결과, 고장 자체보다 **로그가 사실과 다르게 말하는 것**이 문제였음
- 관리자가 화면만 보고 옳은 일을 하게 만듦: 원인 미상 267건에 원인을 붙이고, 모델을 바꿔도 안 풀리는 한도에 모델을 바꾸라고 하지 않고, 로컬에서 난 일을 운영이 막혔다고 말하지 않음

## 범위 밖
- AI 키 한도 자체를 늘리거나 공급자를 추가하는 일 (로그가 옳게 말하고 있고, 그것은 결제·계정 판단)
- ci-discover 호출 감축 (P0030 에서 예산 게이트가 09-20 14:12 결선됨, ci-discover 는 05:55 이후 미실행이라 아직 시험 안 됨 — 다음 실행 때 관측할 일)
- ci-discover 의 `server` 오분류 (09-20 04:07 v0.10.190 에서 `lastReason = 'quota'` 로 이미 고침, 마지막 발생 05:54 는 배포 시차)
- 로컬 dev 가 운영 DB 에 쓰는 것 자체를 끊는 일 (dev 에서도 이 화면이 쓰이므로 끊지 않고 갈라서 봄)

## 완료 정의
- pnpm tsc --noEmit, pnpm test, pnpm build 통과
- 사유 `unknown` 인 ci_job 사건이 새 기록에서 0건이 됨 (재현 입력으로 확인)
- 웹 검색 한도 실패의 화면 문장과 원문이 같은 말을 함
- 「지금 막혀 있는 것」이 운영에서 난 일만 셈

## 참조
- LOOP.md 7절 보안 기준
- apps/web/lib/system-log/narrate.test.ts (기존 가드, 값이 가는지 보는 방식)
- 실측 근거: system_events 18,647행 · ci_job 사건 267건 전부 reason=unknown · db 사유 125건 전부 로컬

## 항목

### I01 우리말로 쓴 오류 문장을 사유로 분류한다
상태: 통과
모드: 경량
범위: apps/web/lib/system-log/reason.ts, apps/web/lib/system-log/narrate.test.ts
감사 기준:
- classifySystemReason({ message: '등록된 AI 공급자가 전부 사용량 한도에 걸렸습니다...' }) 가 'quota' 를 돌려주는 단정이 있다 (지금은 'unknown')
- 우리말 패턴이 영어 패턴보다 **뒤에** 놓여 기존 영어 판정이 안 바뀌는 단정이 있다 (429 를 담은 Prisma 오류가 여전히 'db')
- pnpm --filter web exec node --test lib/system-log/narrate.test.ts 통과
- 보안: 순수 함수의 판정만 바꾼다, 새 데이터·새 창구·외부 입력 없음 — 7절 세 질문 전부 아니오
의존: 없음

### I02 잡이 이미 아는 오류 코드를 분류기에 넘긴다
상태: 통과
모드: 경량
범위: apps/web/lib/ci/jobs/queue.ts, apps/web/lib/system-log/narrate.test.ts
감사 기준:
- queue.ts 가 `new Error(...)` 로 다시 싸면서 버리던 `input.errorCode` 를 분류기가 보는 자리로 넘긴다 (실측: ci_job 사건 267건 전부 reason=unknown 인데 코드는 context.errorCode 에 있었다)
- 가드가 **값이 가는지**를 본다 — `errorCode` 라는 이름이 파일에 있는 것으로 통과하지 않고, 분류 인자로 넘어가는 형태를 대조한다
- pnpm --filter web exec node --test lib/system-log/narrate.test.ts 통과
- 보안: 기록 경로의 인자 하나를 넘길 뿐 새 데이터·창구·외부 입력 없음 — 7절 세 질문 전부 아니오
의존: I01

### I02a 잠금이 풀려 죽은 잡도 관리자 화면에 올린다
상태: 통과
모드: 경량
범위: apps/web/lib/ci/jobs/queue.ts, apps/web/lib/system-log/narrate.test.ts
감사 기준:
- reclaimStalledJobs 가 포기 판정(dead)을 낸 잡을 관리자 로그에 투영한다 (실측 2026-09-20: ci_jobs 에 STALLED dead 322건인데 system_events 에는 0건, 화면에 뜬 267건보다 많다)
- 재시도로 회수한 잡(failed)은 안 올린다 — 다음 차례에 될 수도 있는 것을 사건으로 올리면 진짜 죽은 잡이 그 안에 묻힌다 (기존 finishJob 의 판단과 같은 규칙)
- 가드가 값 기준으로 본다 — 투영 호출이 dead 쪽에만 걸려 있는지 대조하고, 빼 본 판으로 일부러 깨뜨려 실패를 확인한다
- pnpm --filter web exec node --test lib/system-log/narrate.test.ts 통과
- 보안: 기존 표에 기록을 하나 더 남길 뿐 새 표·창구·외부 입력 없음 — 7절 세 질문 전부 아니오
의존: I02

### I03 원인을 모를 때 우리가 쓴 문장을 감추지 않는다
상태: 통과
모드: 경량
범위: apps/web/lib/system-log/narrate.ts, apps/web/lib/system-log/narrate.test.ts
감사 기준:
- reason 이 'unknown' 이고 사람이 읽을 수 있는 한 문장이 있으면 detailOf 가 그 문장을 쓰는 단정이 있다 (실측 원문 「이 영상의 정보를 가져오지 못했습니다. 비공개이거나 삭제되었을 수 있습니다」가 화면에서 「원인을 자동으로 알아내지 못했습니다」로 바뀌고 있었다)
- 스택·영문 예외 문자열처럼 사람 문장이 아닌 것은 기존 문구를 그대로 쓰는 단정이 있다
- pnpm --filter web exec node --test lib/system-log/narrate.test.ts 통과
- 보안: 원문은 이미 maskSecrets 를 지나 저장된 값을 쓴다, 가림이 안 된 새 경로를 열지 않음을 단정으로 확인 — 7절 셋째 질문에 해당하므로 확인함
의존: I01

### I04 웹 검색 한도에 모델을 바꾸라고 하지 않는다
상태: 통과
모드: 경량
범위: apps/web/lib/ci/ai/signals-server.ts, apps/web/lib/system-log/narrate.test.ts
감사 기준:
- signals-server.ts 가 webSearch 를 켜고 부른 실패에 `context.webSearch` 를 실어 보낸다 (실측: 92건이 「모델을 바꾸면 됩니다」라고 했는데 원문은 「모델을 바꿔도 풀리지 않습니다」였다)
- 가드가 손목록이 아니라 **파생 목록**으로 본다 — 기록을 남기는 파일 중 webSearch 를 아는 파일은 전부 그 값을 기록에 실어야 하고, 새 파일이 늘어도 저절로 걸린다
- 값을 뺀 판으로 일부러 깨뜨려 실패를 확인한 사실을 pass --notes 에 적는다
- pnpm --filter web exec node --test lib/system-log/narrate.test.ts 통과
- 보안: 기록에 불리언 하나를 더한다, 비밀·개인정보 아님 — 7절 세 질문 전부 아니오
의존: I01

### I05 로컬에서 난 일을 운영이 막혔다고 말하지 않는다
상태: 통과
모드: 경량
범위: supabase/migrations/272_system_events_env.sql (신규), apps/web/lib/system-log/record.ts, apps/web/app/api/admin/system-log/route.ts, apps/web/app/admin/system-log/SystemLogClient.tsx, apps/web/lib/system-log/narrate.test.ts
감사 기준:
- 마이그레이션이 `env` 칼럼을 더하고 RLS 를 새로 끄지 않는다 (기존 정책 셋이 그대로 남아 있음을 psql 로 확인)
- 기록이 환경을 남기고, 화면 기본값이 운영만 세며, 로컬도 골라 볼 수 있다 (실측: db 사유 125건 전부 로컬인데 화면이 「지금 막혀 있는 것 2건」이라고 빨갛게 말했다)
- pnpm --filter web exec node --test lib/system-log/narrate.test.ts 통과
- 보안: 표에 칼럼을 더하므로 S1 해당 — RLS 가 이미 켜진 표이고 정책이 그대로인지 마이그레이션 적용 뒤 psql 로 확인한다
의존: I01

### I06 판 번호가 다섯 파일에서 같은 말을 하는지 확인한다
상태: 통과
모드: 경량
범위: package.json, apps/web/package.json, .claude/heavy/CEO.md, AGENTS.md, GEMINI.md
감사 기준:
- 다섯 파일이 같은 버전을 말하고, 그 버전이 직전 커밋 버전보다 높다
- apps/web/lib/changelog/entries.ts 는 **안 쓴다** — 이번 판은 /admin/system-log 관리자 전용이고(페이지 requireAdmin · 레이아웃 role 검사 · API 403 셋으로 막힘) 로그인한 일반 사용자가 화면에서 겪는 변화가 없다, 부록 규칙의 「어드민이면 생략하되 이유 한 줄」에 해당하므로 그 사유를 여기 적음
- pnpm --filter web exec node --test lib/policy/version-rule.test.ts lib/policy/policy-sync.test.ts 통과
- 보안: 문서와 판 번호만 본다 — 7절 세 질문 전부 아니오
의존: I05

### I07 이미 쌓인 「원인 미상」 중 증명되는 것만 올린다
상태: 통과
모드: 중량
범위: apps/web/scripts/backfill-system-log-reason.mjs (신규)
감사 기준:
- 대상이 reason='unknown' 이고 새 판정이 unknown 이 아닌 미처리 행뿐이다 (단조 개선이라 멀쩡한 줄을 못 망가뜨린다), 실측 246행
- 전체 재계산은 **하지 않는다** — 저장된 행에 prismaCode·geminiReason 이 없어 재계산이 손실적이고, 전체로 돌리면 16,821행이 제대로 된 사유에서 unknown 으로 떨어진다 (실측)
- 되돌릴 근거를 먼저 파일로 남기고, 그 파일만으로 되돌리는 명령을 보고에 적는다
- 웹 검색 한도 행에 「다른 모델로 바꾸면 됩니다」를 쓰지 않는다 — 원문이 웹 검색이라고 말하면 그 갈래로 문장을 만든다 (안 그러면 백필이 방금 고친 버그를 되살린다)
- 돌린 뒤 psql 로 unknown 잔여와 바뀐 행 수를 실측해 적는다
의존: I05

## 종합 감사

### 네 검사 (2026-09-21)
- pnpm tsc --noEmit: 0 오류
- pnpm lint: 0 오류 (기존 react-hooks/exhaustive-deps 경고만 남음, 내 범위 밖)
- pnpm test: 6,467 / 6,467 통과
- pnpm build: 통과. 첫 시도는 JS heap OOM 으로 죽었고 코드 오류가 아니었음 — NODE_OPTIONS=--max-old-space-size=8192 와 NEXT_DIST_DIR=.next-p0039 로 dev 를 안 덮고 다시 돌려 통과

### 보안 재측정 (7절 「기계가 세는 것」 다섯 줄, psql 실행)
1 RLS 꺼진 public 표: 0
2 anon 이 쓰기 권한을 가진 표: 0
3 TO public 에 USING(true) 인 읽기/전체 정책: 0
4 search_path 안 박힌 SECURITY DEFINER 함수: 0
5 anon 이 읽을 수 있는 SECURITY DEFINER 뷰: 0
마이그 272 적용 뒤 system_events 자체도 확인: RLS 켜짐(t), 정책 3개 그대로, anon 쓰기 권한 0

### 완료 정의 대조
- ci_job 사유 unknown 0건: **부분 달성**. 새 기록은 코드가 잡고(I02), 이미 쌓인 것 중 증명되는 246행은 올렸으나(I07) 97행이 unknown 으로 남음 — React 오류·TypeError 등 우리 사유 아홉 가지 어디에도 안 맞는 것들이라 지어 붙이지 않았음. I03 이 그 줄의 원문 문장을 화면에 그대로 올린다
- 웹 검색 한도 문장과 원문이 같은 말: 달성. 반대 조언 잔여 0 (747행 교정)
- 「지금 막혀 있는 것」이 운영만 셈: 달성. 기본 보기(7일·미처리·운영+모름)에서 critical 2 → 0

### 실측 효과
- system_events 18,647행이 운영 18,385 / 개발 221 / 모름 41 로 갈림
- 미처리 critical 135 → 2 (남은 2건은 2026-09-09 client 오류이고 env 모름이라 일부러 안 숨김, 기본 7일 범위 밖)
- 화면 기본 보기 지문 15 → 13, 그중 「지금 막힘」 2 → 0

### 조사 중 확인했으나 이번에 안 고친 것 (범위 밖 사유 포함)
- AI 키 한도 자체: 로그가 옳게 말하고 있고 결제·계정 판단이라 사람 몫
- ci-discover 호출량: 하루 22,801회(상한 50). 예산 게이트 결선이 09-20 14:12 이고 ci-discover 는 05:55 이후 미실행이라 아직 한 번도 안 걸려 봤음 — 다음 실행 때 관측할 일
- ci-discover 의 server 오분류: 09-20 04:07 v0.10.190 에서 이미 고침, 마지막 발생 05:54 는 배포 시차

### 되돌리기
- 마이그 272: `alter table public.system_events drop column env;` (칼럼만 더했으므로 되돌리면 그 전과 같음)
- I07 데이터 변경: /tmp/system-log-reason-undo-246.json, /tmp/system-log-detail-undo-747.json 의 from 값을 id 별로 UPDATE

## 변경 이력
- v0.1.0 (2026-09-20) 최초 작성 (ins_0049)
- v0.1.1 (2026-09-20) 조사 중 발견: reclaimStalledJobs 가 finishJob 을 안 지나가 잠금 만료로 죽은 322건이 관리자 로그에 한 번도 안 올랐다, 화면에 보이는 267건보다 많은 같은 성격의 구멍이라 항목 I02a 를 추가 (audit:I02)
- v0.1.2 (2026-09-20) I04 기준 정정: gemini-call.ts 에는 webSearch 개념이 아예 없어(웹 검색은 crm/ai/adapters/host.ts 쪽 길) 그 파일을 고치라는 기준이 틀렸음. 손으로 세 자리를 적는 대신 기록 남기는 파일 중 webSearch 를 아는 파일 전부를 파생 목록으로 보는 가드로 바꿈 (audit:I04)
- v0.1.3 (2026-09-20) I06 기준 정정: 이번 판이 전부 관리자 전용 화면이라 entries.ts 에 쓸 사용자 체감 변경이 없음, 부록의 「어드민이면 생략하되 이유 한 줄」로 바꾸고 사유를 항목에 적음 (audit:I06)
- v0.1.4 (2026-09-20) 종합 감사에서 발견: detail·reason 은 기록 시점에 굳는 값이라 이미 쌓인 줄이 옛 문장을 그대로 들고 있음. 전체 재계산은 손실적이라(실측 16,821행 손상) 단조 개선되는 246행만 올리는 항목 I07 추가 (audit:final)
