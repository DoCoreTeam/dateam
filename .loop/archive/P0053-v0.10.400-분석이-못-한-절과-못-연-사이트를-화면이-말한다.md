# PLAN newAX: 분석이 못 한 절과 못 연 사이트를 화면이 말한다
플랜 ID: P0053
플랜 버전: v0.1.1
상태: 완료
지시: iv_0101
목표 버전: v0.10.400
작성: 2026-09-22
시작 커밋: e25e4fb6

## 목표
- 리포트에서 「이상 징후 없음」과 「이상 징후를 못 봤음」이 갈라져 보임
- 분석이 못 만든 절이 몇이고 왜인지가 리포트 첫 화면에 뜸
- 못 연 기관 사이트가 왜 못 열렸는지(인증서·이름·시간 초과·상태) 사유로 남고 화면이 그 말을 함

## 범위 밖
- 분석이 실패한 근본 원인(무료 등급 분당 출력 토큰 상한, 크레딧 마른 키) 고치기 — 키 교체는 P0052 가, AI 설정은 P0050 이 같은 트리에서 쓰는 중이라 안 건드림
- rfp_report_fields 가 전 케이스 0행인 것 (이번 판이 만든 것이 아니고 처음부터 그랬음, 별건)
- 나라장터 서비스 키 등록 (사용자 몫, 화면이 이미 안내함)
- P0001 플랜의 장부 정리 — plan check 가 옛 항목 스물넷의 보안 기준을 요구해 통과가 안 됨, 지나간 판을 다시 쓰는 일이라 안 함

## 완료 정의
- pnpm tsc --noEmit, pnpm lint, pnpm test, pnpm build 통과
- 케이스 e3338eb6 을 열면 못 만든 절 셋(budget·checklist·anomalies)이 사유와 함께 보임 (실브라우저)
- 성공해서 빈 절과 못 만든 절이 화면에서 다르게 보임
- 수집 사유가 fetch_failed 한 덩이가 아니라 갈라져 남음 (KISA 는 인증서 사슬)
- 밖에서 온 오류 원문이 화면에 그대로 안 실림
- 설정값은 env 추가 없이 DB 저장 + UI 관리 (해당 없음, 새 설정 없음)

## 참조
- LOOP.md 7절 보안 기준(S3 오류 메시지에 내부 구조를 싣지 않음, S4 밖에서 온 값), 부록 버전 규칙
- apps/web/app/(rfp)/rfp/[id]/page.tsx loadProgress (지금 job_type·status·error 만 읽음)
- apps/web/lib/rfp/jobs/run-stage.ts runAnalyze (failures 를 잡 progress 에 남김 — 값은 이미 있음)
- apps/web/lib/rfp/radar/site-collect.ts fetchPage (지금 실패를 전부 fetch_failed 로 뭉갬)
- 실측 2026-09-22: 케이스 e3338eb6 잡 progress.failures 셋, KISA 사슬 길이 1(중간 인증서 누락)

## 항목

### I01 못 만든 절을 화면이 말한다
상태: 통과
모드: 경량
범위: apps/web/app/(rfp)/rfp/[id]/page.tsx, apps/web/app/(rfp)/rfp/[id]/ReportClient.tsx, apps/web/lib/rfp/terms.ts, apps/web/lib/rfp/analyze/failure-reason.ts (신규), apps/web/lib/rfp/analyze/failure-reason.test.ts (신규)
감사 기준:
- 보안: 공급자 오류 원문을 화면에 안 싣고 정해진 사유 몇 개로 접어 보냄 (조직 id·과금 주소가 실제 원문에 있음, 실측)
- loadProgress 가 끝난 analyze 잡의 progress.failures 를 읽어 화면에 넘김
- 실패가 0이면 그 자리가 안 뜨는 단정
- node --test failure-reason 통과
의존: 없음

### I02 「없음」과 「못 봤음」을 절마다 가른다
상태: 통과
모드: 경량
범위: apps/web/app/(rfp)/rfp/[id]/ReportClient.tsx, apps/web/lib/rfp/terms.ts
감사 기준:
- 못 만든 절은 빈 목록 대신 사유 줄을 그림 (실측 케이스의 anomalies·checklist·budget)
- 성공했는데 0건인 절은 지금처럼 「없음」
- 일부러 깨기: 실패 목록을 비운 판에서 「없음」으로 돌아오는 것을 확인하고 되돌림
의존: I01

### I03 못 연 사이트는 왜 못 열었는지 말한다
상태: 통과
모드: 경량
범위: apps/web/lib/rfp/radar/site-collect.ts, apps/web/lib/rfp/radar/site-collect.test.ts, apps/web/components/rfp/SourceSites.tsx, apps/web/lib/rfp/terms.ts
감사 기준:
- 보안: 밖에서 온 오류 문구를 그대로 안 싣고 정해진 사유 이름으로만 남김
- fetchPage 가 인증서·이름 못 찾음·연결 거부·시간 초과·HTTP 상태를 갈라 남김
- 실측 대조: KISA 는 tls 사유로 남는다 (지금은 fetch_failed)
- pnpm test site-collect 통과
의존: 없음

### I03a 죽은 잡의 사유도 접는다
상태: 통과
모드: 경량
범위: apps/web/app/(rfp)/rfp/[id]/page.tsx, apps/web/app/(rfp)/rfp/[id]/ReportClient.tsx
감사 기준:
- 보안: deadJob.error 가 공급자 원문 그대로 화면에 가던 길을 막고 접힌 사유로 바꿈 (완료 정의의 「밖에서 온 오류 원문이 안 실림」이 이 길에서 새고 있었음)
- 사유를 못 접는 문구는 지금 문구(analysisFailedDesc)로 떨어짐
- 일부러 깨기: 원문을 담은 죽은 잡을 만들어 화면에 원문이 안 나오는 것을 확인
의존: I01

### I04 가드와 판 올리기
상태: 통과
모드: 경량
범위: apps/web/lib/policy/rfp-silent-failure.test.ts (신규), 루트 package.json, apps/web/package.json, .claude/heavy/CEO.md, AGENTS.md, GEMINI.md, apps/web/lib/changelog/entries.ts
감사 기준:
- 가드가 「끝난 분석의 failures 를 화면이 읽는다」를 단정하고, 일부러 깨뜨려 실패를 확인하고 되돌림
- 새 시험이 pnpm test 에 실제로 도는 것을 총 테스트 수 증가로 확인
- 버전 여섯 파일이 같은 값
의존: I01, I02, I03

## 종합 감사

검사 넷 (2026-09-22)

- pnpm tsc --noEmit: 통과 (exit 0, 출력 0줄)
- pnpm lint: 통과 (exit 0, Error 0건, 기존 경고 둘은 내 범위 밖 useFormCore·useTour)
- pnpm test: 6712 중 6711 통과, 실패 1 — **내 것이 아님**
  - 실패한 것은 lib/access/decide.test.ts 「CRM 판정이 이 판 앞뒤로 같다」
  - 같은 트리에서 도는 옆 세션의 미커밋 변경(lib/access/capabilities.ts, lib/crm/auth/capabilities-gate.ts, lib/terms/access.ts)이 원인
  - 내 커밋만 있는 판에서 초록인 것을 따로 확인: HEAD 로 별도 worktree 를 떠서 같은 시험을 돌려 40/40
- pnpm build: 통과 (exit 0, Compiled successfully 47s, 정적 294/294)
  - NODE_OPTIONS=--max-old-space-size=8192 NEXT_DIST_DIR=.next-p0053 로 돌려 옆 세션의 dev 서버를 안 건드림

보안 다섯 줄 (docs/policy/security-count.sql 실행)

- rls_off_tables 0
- anon_write_tables 0
- public_using_true_policies 0
- unpinned_secdef_functions 0
- anon_readable_secdef_views 0

완료 정의 대조

- 검사 넷 통과: 충족 (test 의 실패 1건은 위와 같이 옆 세션 것)
- 케이스 e3338eb6 에 못 만든 절 셋이 사유와 함께 보임: 충족 (실측 :3100, 배너 1 + 절 단위 3, 사유 문구 6회)
- 성공해서 빈 절과 못 만든 절이 다르게 보임: 충족
  - 같은 케이스에서 못 만든 셋(anomalies·budget·checklist)은 카드가 서고, missing 을 빈 배열로 바꾼 판에서는 그 셋이 통째로 안 그려짐 — 성공해서 빈 절이 보이는 모습이 그것임
- 수집 사유가 갈라져 남음: 충족 (훑기 재실행에서 KISA 가 fetch_failed → tls, 화면은 사람 말)
- 밖에서 온 오류 원문이 화면에 안 실림: 충족
  - 못 만든 절은 접힌 사유만 감(단정으로 확인), 죽은 잡도 I03a 에서 같은 길로 접음
  - 원문을 담은 죽은 잡을 실제로 넣고 화면에 org_·console.groq.com·원문 0건인 것을 확인한 뒤 그 줄을 지움
- 새 설정 없음(env 추가 없음): 충족

전체 diff (내 커밋 다섯 1a06ae6c 14e4126e 8f0b8c8b be607f65 3c009df4)

- 손댄 파일 열다섯: RFP 리포트 화면 둘, 수집 화면 하나, 말 파일 하나, 새 모듈과 그 시험 둘,
  수집 모듈과 그 시험 둘, 새 가드 하나, 발행 파일 하나, 버전 파일 다섯
- 범위 밖 변경 0. 같은 트리에서 도는 다른 세션의 파일(lib/ai-chat/probe-*, lib/access/*, tsconfig)은 안 건드림
- 비밀 0. 다만 시험 지문에 Groq 조직 id 가 그대로 들어가 있던 것을 가림(org_01EXAMPLE…) —
  비밀은 아니지만 이번 판이 화면에서 없애자던 바로 그 내부 식별자라 저장소에도 안 남김
- 하드코딩 문자열 0: 화면 문구는 전부 lib/rfp/terms 에서 옴

항목 대 결과 대조

- I01 -> lib/rfp/analyze/failure-reason.ts 신설(8단정), page.tsx 가 progress 를 읽어 missing 으로 넘김
- I02 -> ReportClient 가 절마다 「없음」과 「못 봤음」을 가름, 이상 조항 0건도 갈래를 가짐
- I03 -> site-collect 가 tls·dns·refused·connection_lost·timeout 을 가름(25→31단정), 화면은 사람 말
- I03a -> deadJob 도 classifyFailure 를 지남, 원문이 가는 길 없음
- I04 -> lib/policy/rfp-silent-failure.test.ts 6단정, 다섯 규칙 전부 일부러 깨서 확인, 등재 544→545

발견 사항

- 가드를 처음 짰을 때 `missingSome` 이 `missingSomeDesc` 의 앞부분이라 부분 문자열로 통과했다.
  일부러 깨뜨리지 않았으면 초록인 채로 남았을 것이다 — 화면으로 나가는 모양까지 재도록 고쳤다
- 이 판이 고친 것은 **말하는 쪽**이다. 못 만든 진짜 원인(무료 등급의 분당 출력 토큰 상한 1000 에
  1333 요청, 크레딧이 마른 키)은 그대로 남아 있다. 키 교체는 P0052, AI 설정은 P0050 이 같은 트리에서
  쓰고 있어 범위 밖으로 두었다
- rfp_report_fields 는 다섯 케이스 전부 0행이다. 이번 판이 만든 것이 아니라 처음부터 그랬고,
  리포트 본문은 rfp_report_versions.report 에 들어 있다 — 별건으로 남긴다

## 부록 P0001 K04 실측 (2026-09-22, 이 판에서 돌린 결과)

훑기 POST /api/rfp/radar, 14.5초, swept 22, hits 4

| 사이트 | 종류 | 결과 | 사유 |
|---|---|---|---|
| 나라장터 | g2b | 0건 | no_service_key, 서비스 키 미등록 (화면이 안내함) |
| 한국지능정보사회진흥원(NIA) | 자바스크립트 게시판 | 6건 수집 | titlesOnly, 목록에서 상세 주소를 못 얻음 (채택 때 복원함) |
| 한국인터넷진흥원(KISA) | 일반 게시판 | 0건 | fetch_failed, 실제 원인은 중간 인증서 누락 |

첨부 있는 공고 1건 전 과정 (NIA 조달입찰공고 → 케이스 e3338eb6)

- 채택 201, 11.6초. 상세 주소 복원 성공 View.do?cbIdx=78336&bcIdx=30002
- 첨부 둘 내려받음 (공고서 hwpx, 제안요청서 hwpx), 실패 0
- 워커 parse 11.0초 → structure 1.3초 → index 9.5초 → analyze 88.1초, 넷 다 ok
- DB 결과 stage=reported, 파일 2(경로 채움 2), IR 2, 섹션 71, 블록 273, 리포트 6,968자, 채운 칸 15
- 사업명을 문서에서 뽑아 케이스 제목을 바꿈 (title_confirmed=false 라 덮음)

실패한 것 (감추지 않음)

- 분석 일곱 작업 중 셋(budget·checklist·anomalies)이 못 돌았음
- 사유는 Groq qwen/qwen3.8-27b 의 분당 출력 토큰 상한 1000 에 1333 요청, 모델 셋 다 실패
- 원장에 크레딧 소진(429)과 분당 한도 차단도 섞여 있음
- 사유가 잡 progress 에는 남았는데 **화면이 그 칸을 안 읽음** — 이 플랜이 고치는 것

## 변경 이력
- v0.1.0 (2026-09-22) 최초 작성 (iv_0101), P0001 K04 를 실제로 돌려 나온 결함으로 항목을 세움
- v0.1.1 (2026-09-22) 죽은 잡의 사유는 아직 공급자 원문을 그대로 화면에 싣고 있었다. 내 완료 정의에 걸리는 길이라 I03a 를 I04 앞에 끼움 (audit:I03)
- v0.1.1 (2026-09-22) 죽은 잡의 사유가 공급자 원문 그대로 화면에 가는 길이 남아 있어 I03a 를 끼움 (audit:I03)
