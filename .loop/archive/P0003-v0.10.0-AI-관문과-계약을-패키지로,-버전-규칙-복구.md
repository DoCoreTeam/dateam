# PLAN newAX: AI 관문과 계약을 패키지로, 버전 규칙 복구
플랜 ID: P0003
플랜 버전: v0.1.14
상태: 완료
지시: iv_0039
목표 버전: v0.10.0
작성: 2026-09-10
시작 커밋: 591754ab

## 목표
- AI 를 부를 때 지켜야 할 것(시간 제한, 개인정보 가림, 폴백, 비용, 전송 기록)이 호출부마다 흩어져 있던 것을 패키지 한 곳으로 모음
- 능력 여덟이 같은 모양으로 답하도록 결과 계약을 타입으로 박음
- 벤더 주소를 직접 부르는 파일 35개를 기준선으로 잠가 더 늘지 않게 함
- 커밋마다 어긋나 있던 버전 규칙을 되살려 사용자 화면 업데이트 내역이 다시 발행되게 함

## 범위 밖
- 벤더 직접 호출 35건을 실제로 관문 안으로 옮기는 일 (기획서 4단계)
- AI 부품 한 벌과 짝 가드 (기획서 3단계)
- 계약 판 번호를 실제 저장 자리에 동봉하는 일 (기획서 2단계)
- 공공데이터포털 서비스 개방, RFP 용어집 이관, 카탈로그 신설
- 패키지 npm 발행

## 완료 정의
- pnpm tsc --noEmit, pnpm lint, pnpm test, pnpm build 통과
- packages 안에 한글 문자열 0건, 사용자 노출 문구는 쓰는 쪽이 준다
- 설정값은 env 추가 없이 DB 저장과 UI 관리 (해당 시)
- 벤더 직접 호출 파일 수가 기준선 35 를 넘지 않음
- package.json 버전과 changelog entries.ts 맨 위 버전이 같음

## 참조
- docs/2026-09-10-ai-common-layer/README.md (1단계, 2절 결과 계약, 3절 패키지 경계, 12절 놓치기 쉬운 것)
- LOOP.md 6절 산출물과 문체, 부록 프로젝트 관례
- apps/web/scripts/changelog-gen.mjs (버전이 올라야 커밋을 집는다)

## 항목

### I00 타입 검사를 초록으로 되돌린다
상태: 통과
모드: 경량
범위: apps/web/lib/rfp/radar/js-download.ts
감사 기준:
- 실측 근거를 notes 에 적음: pnpm tsc --noEmit 이 이미 실패 상태이고 오류는 js-download.ts 87줄 TS2802 하나, 마지막 커밋 591754ab 에 들어온 것이라 이 플랜 이전부터 빨강
- 이 항목 없이는 어느 항목도 자가감사 3b 정적 검사를 통과할 수 없으므로 맨 앞에 둠
- tsconfig 는 안 건드림 (target 미설정이 근원이지만 다른 세션이 tsconfig.json 을 고치는 중이라 부딪힘)
- pnpm tsc --noEmit 오류 0건
- 변경 파일이 js-download.ts 하나뿐인 것을 git status 로 확인
의존: 없음

### I01 버전 규칙을 부록에 명문화하고 뒷걸음을 막는다
상태: 통과
모드: 경량
범위: LOOP.md, scripts/loop.mjs
감사 기준:
- 실측 근거를 notes 에 적음: git log -60 에서 완료형 커밋 메시지 v0.8.0 이 17건, 즉 버전 하나를 17번 재사용했고 그동안 package.json 은 0.8.0 에서 안 움직임
- 원인을 notes 에 적음: apps/web/scripts/changelog-gen.mjs 가 버전이 올라야 커밋을 집으므로 재사용된 17건은 한 건도 게시되지 않음
- LOOP.md 부록에 버전 규칙 다섯 줄 추가: 항목 커밋은 목표버전-Ixx 이고 package.json 을 안 건드림, 완료 커밋만 package.json 을 올림, 플랜 밖 단독 커밋은 patch 를 올리고 vX.Y.Z 형식으로 적고 같은 커밋에 entries.ts 블록을 넣음, 동시에 도는 플랜은 목표 버전을 겹치지 않게 잡음, 버전은 뒤로 가지 않음
- git diff LOOP.md 에서 부록 절 밖 변경이 0줄
- scripts/loop.mjs final 이 목표 버전을 package.json 에 무조건 대입하던 것을 현재 버전보다 높을 때만 쓰도록 바꿈, 낮은 목표로 호출해도 안 내려가는 것을 확인
- LOOP_PLAN_FILE 미설정 시 .loop/PLAN.md 로 동작하는 것을 node scripts/loop.mjs resume 출력이 P0002 인 것으로 확인
- 한자 0건, 이모지 0건, 가운뎃점 0건
의존: I00

### I01a 부록을 기존 여섯 파일 체크리스트에 맞춘다
상태: 통과
모드: 경량
범위: LOOP.md, scripts/loop.mjs
감사 기준:
- 실측 근거를 notes 에 적음: 기존 버전 규칙이 GEMINI.md 813줄 버전 업데이트 체크리스트에 이미 있고 AGENTS.md 와 .claude/heavy/CEO.md 에도 같은 내용이 있음, I01 부록은 package.json 둘만 적어 정책 3파일과 entries.ts 넷을 빠뜨림
- 왜 안 지켜졌는지 notes 에 적음: loop-kit 이 들어오면서 LOOP.md 만 읽는 세션은 그 체크리스트를 볼 길이 없고 CLAUDE.md 는 LOOP.md 를 가리키는 어댑터라 버전 카드가 없음
- 부록에 다음 버전 셈법을 기존 규칙대로 적음: 다음 버전은 package.json 버전과 최근 커밋 버전 중 큰 쪽에 patch 1 을 더한 값, git log 확인 없이 package.json 만 보고 정하지 않음
- 부록에 올릴 파일 여섯을 순서대로 적음: 루트 package.json, apps/web/package.json, .claude/heavy/CEO.md, AGENTS.md, GEMINI.md, apps/web/lib/changelog/entries.ts
- 이미 있는 가드 apps/web/lib/policy/policy-sync.test.ts 가 앞 다섯을 본다는 것을 부록에 적어 새 가드가 겹치지 않게 함
- 부록이 여섯 파일이라 적으면 CLI 도 여섯을 올려야 하므로 loop.mjs final 이 정책 3파일의 버전 줄도 같이 올림, 격리 저장소에서 여섯 파일이 다 바뀌는 것을 확인
- git diff LOOP.md 에서 부록 절 밖 변경이 0줄
- 한자 0건, 이모지 0건, 가운뎃점 0건, 전각 대시 0건
의존: I01

### I02 버전 규칙 가드
상태: 통과
모드: 경량
범위: apps/web/lib/policy/version-rule.test.ts (신규), apps/web/lib/changelog/entries.ts, apps/web/package.json, .github/workflows/design-guard.yml
감사 기준:
- 기존 규칙이 entries.ts 블록을 사용자 체감 변경이 있을 때만 요구하므로 두 버전이 늘 같지는 않음, 따라서 같음이 아니라 entries.ts 맨 위가 루트 package.json 보다 높으면 실패로 잡음 (발행이 코드보다 앞설 수는 없음)
- entries.ts 버전 목록에 중복 0건이고 내림차순인 것을 확인
- git 이 닿을 때는 기준선 커밋 이후 완료형 커밋의 버전이 루트 package.json 보다 높으면 실패 (커밋만 올리고 package.json 을 안 올린 경우)
- 기준선 커밋 이후 완료형 커밋에서 같은 버전이 2회 이상 나오면 실패, 지금 쌓인 17회는 기준선 아래로 흡수
- policy-sync.test.ts 가 이미 보는 것(정책 3파일과 package.json 둘의 버전 일치)은 다시 보지 않음, 이 가드는 entries.ts 와의 일치와 버전 재사용만 봄
- CI 는 얕은 복제라 기준선 커밋에 못 닿음, .github/workflows/design-guard.yml 체크아웃에 fetch-depth 0 을 넣어 CI 에서도 git 검사가 실제로 돌게 함
- git 검사를 건너뛰는 경우에는 이유를 출력함, 조용히 통과하지 않음
- 가드가 처음 돌자마자 잡은 기존 결함 셋을 무손실로 고침: 0.7.616 블록 두 개 중복, 0.7.602 다음에 0.7.603, 0.7.451 다음에 0.7.452
- 병합과 순서 바꾸기 전후로 블록 수와 항목 수를 세어 하나도 안 사라진 것을 확인
- node --test 로 단독 통과
- apps/web/package.json test 스크립트에 등재, 등재 전후 총 테스트 수를 둘 다 notes 에 적어 실제로 늘어난 것을 확인
의존: I01a

### I03 밀린 열여섯 커밋을 사용자 화면에 발행한다
상태: 통과
모드: 경량
범위: LOOP.md, package.json, apps/web/package.json, .claude/heavy/CEO.md, AGENTS.md, GEMINI.md, apps/web/lib/changelog/entries.ts
감사 기준:
- 수를 다시 셈: git rev-list --count 05c0a31e..591754ab 로 16건, 앞서 적은 17은 git log -60 창이 밀려 생긴 오차이므로 바로잡음
- v0.8.0 완료 커밋 05c0a31e 이후 16건 중 사용자가 화면에서 체감하는 것만 골라 0.8.1 블록으로 작성
- 항목 커밋이 여섯 파일을 안 건드린다는 부록 규칙의 예외임, 이 항목은 이번 플랜의 일이 아니라 지난 판의 밀린 발행을 되살리는 것이므로 그 예외를 부록에 한 줄로 적음
- 백엔드나 관리자 전용 변경은 제외하고 제외 사유를 notes 에 한 줄
- 기존 체크리스트대로 여섯 파일을 다 올림 (루트 package.json, apps/web/package.json, .claude/heavy/CEO.md, AGENTS.md, GEMINI.md, entries.ts), 쓴 직후 JSON 파싱으로 0바이트가 아닌 것을 확인
- policy-sync.test.ts 통과 (정책 3파일이 같이 안 오르면 여기서 걸림)
- I02 가드 통과
의존: I02

### I04 패키지 자리를 하나 만들어 세 명령으로 성립을 확인한다
상태: 통과
모드: 경량
범위: pnpm-workspace.yaml, packages/ai-core (신규 3파일), apps/web/next.config.js, apps/web/package.json, apps/web/lib/rfp/ai/gateway.ts
감사 기준:
- 실측 근거를 notes 에 적음: node_modules 안에 실체 디렉터리로 있는 ts 파일은 ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING 으로 죽고 pnpm 이 만드는 심볼릭 링크면 통과 (2026-09-10 확인), 그래서 이 저장소에서 packages 는 링크로만 성립함
- pnpm install 후 apps/web/node_modules 안 패키지 경로가 심볼릭 링크인 것을 test -L 로 확인
- 계약 판 번호 상수를 관문의 CallMeta 에 실제로 실어 보냄, 쓰이지 않는 import 로 통과시키지 않음 (빌드가 안 쓰는 모듈을 털어내면 아무것도 증명 못 함)
- gateway.test.ts 는 meta 를 칸별로 보고 통째로 비교하지 않으므로 칸 하나 늘어도 기존 단정이 그대로 통과하는 것을 확인
- pnpm tsc --noEmit, pnpm test, pnpm build 셋 다 통과
- next.config.js transpilePackages 에 패키지 이름 등재 (Next 14.2.29 는 워크스페이스 ts 를 자동으로 컴파일하지 않음)
의존: I01

### I04a CI 가 설치 단계에서 죽던 것을 고친다
상태: 통과
모드: 경량
범위: .github/workflows/design-guard.yml, .github/workflows/changelog-gen.yml, .github/workflows/gcube-price-check.yml
감사 기준:
- 실측 근거를 notes 에 적음: gh run list 로 design-guard 최근 5회, changelog-gen 최근 4회, gcube-price-check 최근 3회가 전부 실패이고 모두 15에서 22초 만에 끝남, 실패 지점은 pnpm install --frozen-lockfile 의 ERR_PNPM_LOCKFILE_CONFIG_MISMATCH
- 워크플로 셋이 같은 원인이므로 셋 다 고침, 특히 changelog-gen 은 업데이트 내역을 발행하는 기계라 이것이 죽어 있던 것도 발행이 멎은 원인 하나임
- 정정을 notes 에 적음: I02 에서 fetch-depth 0 을 넣으면 CI 에서 가드가 돈다고 적었으나 CI 는 그 앞 설치 단계에서 죽으므로 그 말은 틀렸음, 이 항목이 통과해야 그 말이 참이 됨
- pnpm-lock.yaml 이 .gitignore 29줄로 일부러 빠져 있는 것을 확인 (세션이 여럿이라 충돌을 피하려는 선택으로 보임), 그 선택을 뒤집지 않고 워크플로 쪽을 고침
- 워크플로 설치 명령을 잠긴 설치가 아닌 것으로 바꿈
- push 가 금지라 CI 를 직접 볼 수 없으므로 CI 와 같은 조건으로 재현해 확인함: 락파일 없는 새 워크트리에서 잠긴 설치는 CI 와 똑같은 ERR_PNPM_LOCKFILE_CONFIG_MISMATCH 로 실패하고 고친 명령은 성공해야 함
- 사용자가 push 한 뒤 gh run 으로 실제 통과를 다시 볼 것을 보고에 적음
의존: I04

### I05 결과 계약 한 벌
상태: 통과
모드: 경량
범위: packages/ai-core/src/contract.ts (신규), packages/ai-core/src/capability.ts (신규), packages/ai-core/src/contract.test.ts (신규), packages/ai-core/src/index.ts, apps/web/package.json
감사 기준:
- 칸 일곱이 타입으로 있음: 값, 근거(원문 블록 번호와 글자 자리), 확신, 출처(공급자 모델 시각), 상태, 고친 흔적, 계약 판 번호
- 능력 여덟이 타입으로 있음: 추출 요약 판정 제안 생성 질의응답 전사 의미검색
- 상태에 흘려보내는 중간값이 처음부터 있음, 나중에 넣으면 부품을 다시 만들게 됨
- 계약 판 번호는 값을 만들 때 반드시 받는 자리이고 기본값으로 채우지 않음
- node --test 통과, apps/web/package.json test 스크립트에 등재
의존: I04

### I06 관문을 패키지로 올린다
상태: 통과
모드: 중량
범위: packages/ai-gateway (신규), apps/web/lib/rfp/ai/gateway.ts, apps/web/lib/rfp/ai/mask.ts, apps/web/lib/ai/json-recover.ts, apps/web/lib/rfp/ai/models.ts, apps/web/package.json
감사 기준:
- gateway 240줄, mask 110줄, json-recover 97줄이 packages/ai-gateway 로 가고 저장소에 남는 파일은 재수출과 우리 회사 사실 주입만 남음, 관문 논리(순서, 폴백, 기록, 자리표)는 한 줄도 저장소에 안 남음
- 문서 등급 판정은 우리 회사 사실이므로 패키지가 gate 함수로 받고 저장소 껍데기가 주입함, 호출처 넷은 그대로 두 인자만 넘김
- 비용 계산이 두 벌이 되지 않게 costKrw 도 패키지로 옮기고 models.ts 는 재수출만 남김 (중량 규정 재사용 단일구현)
- 패키지 안 한글 0건, JsonRecoverError 의 한글 메시지를 영어로 바꾸되 그 문구가 화면에 안 나가는 것을 호출처에서 확인
- 이 셋을 참조하는 파일 35개는 한 줄도 안 바뀜, git status 에 안 뜨는 것으로 확인
- 기존 gateway.test.ts 와 json-recover.test.ts 의 단정이 그대로 통과
- 개인정보 원문 표는 패키지 밖으로 내보내지 않음
의존: I05

### I07 공급자 명세와 모델을 패키지로 올린다
상태: 통과
모드: 경량
범위: packages/ai-providers (신규), apps/web/lib/rfp/ai/models.ts, apps/web/lib/ai/provider-catalog.ts, apps/web/package.json, apps/web/next.config.js
감사 기준:
- 경계를 실물에 맞춰 다시 그음: 두 파일은 우리 사실과 벤더 사실이 한 배열에 섞여 있어 통째로 옮길 수 없음, 그래서 재수출이 아니라 갈라서 옮김
- 패키지로 가는 것: 벤더 주소, 키 접두사, 벤더 능력, 키 발급 주소, 키 접두사 판별 규칙(sk 와 sk-ant 가 겹치는 문제), 모델 고르기 규칙
- 저장소에 남는 것: 화면에 적는 이름, org_content META 키 자리, 기본 모델 선택, 용도 문구, DB 행을 우리 모양으로 바꾸는 toModel, 우리 등급 어휘
- provider-catalog 를 쓰는 파일 열다섯의 import 이름이 하나도 안 바뀜 (git status 로 확인)
- provider-catalog.test.ts 가 가상의 여섯째 공급자로 확장성을 확인하는 단정을 그대로 통과
- provider-catalog.test.ts 기존 단정 통과
- 정정: I04 에서 transpilePackages 에 안 올리면 빌드가 깨진다고 적었으나 실측으로 안 올려도 통과함, 심볼릭 링크가 저장소 안 경로로 풀려 Next 기본 규칙에 걸리기 때문, 그 통과는 우연이므로 세 패키지를 다 올리고 이유를 주석에 적음
- 패키지 안 한글 0건
의존: I06

### I08 기록 창구는 기본값이 없다
상태: 통과
모드: 경량
범위: packages/ai-gateway/src/store.ts (신규), packages/ai-gateway/src/gateway.ts, packages/ai-gateway/src/index.ts, packages/ai-gateway/src/store.test.ts (신규), apps/web/app/api/rfp/worker/tick/route.ts, apps/web/app/api/rfp/profile/draft/route.ts, apps/web/app/api/rfp/radar/rules/draft/route.ts, apps/web/package.json
감사 기준:
- 기록 창구를 안 주면 타입 검사에서 막히는 것은 이미 참이므로 그것만으로는 부족함, 진짜 구멍은 빈 함수 몸통이었음 (실측: radar/rules/draft 의 recordTransfer 가 빈 몸통이라 그 길의 전송은 아무 데도 안 남음)
- 창구가 영수증을 돌려주게 바꿔 빈 몸통이 타입 검사에서 막히게 함, 안 남긴 것은 사유를 적어 돌려주는 길로만 가능하게 함
- 영수증을 CallMeta 에 실어 호출처가 「안 남았다」를 볼 수 있게 함, 조용히 0건이 되는 길이 없음
- 안 남긴 사유를 빈 문자열로 넘기면 막힘
- 전송 기록이 지금 0건인 이유를 notes 에 적음
- node --test 통과, 등재
의존: I06

### I09 패키지 경계 가드
상태: 통과
모드: 경량
범위: apps/web/lib/policy/ai-package-boundary.test.ts (신규), apps/web/package.json
감사 기준:
- packages 안 소스에 한글 0건 (주석 포함), 일부러 한글 한 줄 넣어 실패하는 것을 확인
- packages 가 apps/web 을 import 하는 곳 0건, 일부러 넣어 실패하는 것을 확인
- 가드를 좁게 잡아 packages 밖 파일은 안 봄
- 등재 후 총 테스트 수 증가 확인
의존: I07

### I10 벤더 직접 호출 기준선
상태: 통과
모드: 경량
범위: apps/web/lib/policy/vendor-call-baseline.test.ts (신규), apps/web/lib/policy/vendor-call-baseline.json (신규), apps/web/package.json
감사 기준:
- 벤더 주소를 직접 부르는 파일 목록을 실측으로 확정해 json 에 박음, 지금 값 35 (테스트 제외)
- 목록 밖 파일이 새로 생기면 실패, 시험용 파일 하나 만들어 실패하는 것을 확인하고 지움
- 목록에 있던 파일이 관문으로 옮겨 사라지면 기준선을 따라 내리도록 안내 문구가 실패 메시지에 있음
- 등재 후 총 테스트 수 증가 확인
의존: I09

## 종합 감사

실행한 명령과 결과 (2026-09-14)

- pnpm tsc --noEmit 통과, 오류 0건
- pnpm lint 통과, exit 0 (경고만, 기존 react-hooks 경고)
- pnpm test 통과, 5449건 전부 통과, 실패 0
- NEXT_DIST_DIR=.next-check pnpm build 통과, Compiled successfully

완료 정의 대조

- 세 명령 전부 통과
- packages 안 한글 0건 (주석까지), 가드 ai-package-boundary 가 잠금
- 벤더 직접 호출 29개로 기준선과 같음, 가드 vendor-call-baseline 이 잠금
- package.json 0.8.1 과 entries.ts 맨 위 0.8.1 일치, 가드 version-rule 이 잠금
- 설정값 env 추가 0건

전체 diff 검토

- 시작 커밋 591754ab, 항목 커밋 13개, 건드린 파일 47개
- 범위 밖 변경 0건, 다른 세션이 같은 트리에서 고치던 ai-chat providers 와 PLAN.md 와 tsconfig.json 은 한 번도 커밋에 안 실림 (커밋마다 git show --stat 로 확인)
- diff 에 키 토큰 비밀번호 0건
- 각 항목 범위의 파일이 실제로 존재하거나 변경됨

발견 사항 (계획에 없던 것 다섯)

- 타입 검사가 이 플랜 이전부터 빨강이었음 (js-download.ts TS2802), 항목 I00 으로 먼저 고침
- 기존 버전 규칙이 정책 3파일에 여섯 파일 체크리스트로 이미 있었고 LOOP.md 만 읽는 세션은 볼 길이 없었음, I01a 로 부록에 옮김
- entries.ts 에 기존 결함 셋 (0.7.616 중복, 0.7.602 와 0.7.451 순서 역전), 가드를 처음 돌리자마자 잡혀 무손실로 고침
- 워크플로 셋이 전부 설치 단계에서 죽고 있었음, changelog-gen 도 죽어 있어 발행이 멎은 원인 하나였음, I04a 로 고침
- 기획서의 벤더 직접 호출 34는 Drive 와 YouTube 연동 다섯을 함께 센 값이었음, AI 끝점만 세면 29

남은 것

- 사용자가 push 한 뒤 gh run 으로 워크플로 셋이 실제로 설치 단계를 넘는지 확인 필요 (push 는 금지라 여기서 못 봄)
- apps/web/tsconfig.json 이 시험 파일을 제외하므로 시험 안의 가짜 기록 창구는 여전히 빈 몸통이 가능함, 앱 코드에서는 막힘
- 벤더 직접 호출 29개를 실제로 관문 안으로 옮기는 일은 기획서 4단계

## 변경 이력
- v0.1.0 (2026-09-10) 최초 작성 (iv_0039)
- v0.1.1 (2026-09-09) 타입 검사가 이 플랜 이전부터 빨강이라 어느 항목도 정적 검사를 통과할 수 없음, 복구 항목 I00 을 맨 앞에 넣음 (audit:I01)
- v0.1.2 (2026-09-09) 기존 버전 규칙이 정책 3파일에 여섯 파일 체크리스트로 이미 있었음, I01 부록이 넷을 빠뜨려 보완 항목 I01a 를 넣고 I02 I03 기준을 그것에 맞춤 (audit:I01)
- v0.1.3 (2026-09-09) 부록이 여섯 파일이라 적었는데 CLI 는 둘만 올려 규칙과 도구가 어긋남, I01a 범위에 scripts/loop.mjs 를 넣음 (audit:I01a)
- v0.1.4 (2026-09-09) 기존 규칙이 entries.ts 를 사용자 체감 변경 때만 요구하므로 버전 같음 단정은 틀림, 앞서감 금지로 바꾸고 CI 얕은 복제 문제를 fetch-depth 로 고치도록 범위에 워크플로 추가 (audit:I02)
- v0.1.5 (2026-09-09) 가드를 처음 돌리자 entries.ts 기존 결함 3건이 나옴, 무손실 수리를 I02 범위에 넣음 (audit:I02)
- v0.1.6 (2026-09-09) 재사용 커밋 수를 16으로 바로잡고 밀린 발행이 항목 커밋 규칙의 예외임을 부록에 적도록 I03 범위에 LOOP.md 추가 (audit:I03)
- v0.1.7 (2026-09-09) 패키지 성립을 증명하려면 실제로 쓰이는 소비처가 있어야 함, 관문 CallMeta 에 계약 판 번호를 실어 보내도록 I04 범위에 apps/web/package.json 과 gateway.ts 추가 (audit:I04)
- v0.1.8 (2026-09-09) design-guard CI 가 최근 5회 전부 설치 단계에서 죽고 있어 I02 가드가 CI 에서 한 번도 안 돌았음, 고치는 항목 I04a 추가 (audit:I04)
- v0.1.9 (2026-09-09) 죽은 워크플로가 셋이고 원인이 같음, changelog-gen 도 죽어 있어 발행 실패의 원인 하나였음 (audit:I04a)
- v0.1.10 (2026-09-09) push 금지라 CI 관측 불가, 같은 조건 재현으로 감사 기준을 바꿈 (audit:I04a)
- v0.1.11 (2026-09-09) 중량 규정의 재사용 단일구현에 걸려 costKrw 도 같이 옮겨야 함, 문서 등급은 우리 사실이라 주입으로 바꿈 (audit:I06)
- v0.1.12 (2026-09-09) 두 파일이 우리 사실과 벤더 사실을 한 배열에 섞고 있어 통째 이관이 불가, 갈라서 옮기도록 기준을 고침 (audit:I07)
- v0.1.13 (2026-09-14) transpilePackages 없이도 빌드가 통과함을 실측, I04 의 단정을 정정하고 세 패키지를 올림 (audit:I07)
- v0.1.14 (2026-09-14) 창구 주입 강제는 이미 참이라 부족함, 빈 함수 몸통이 진짜 구멍이므로 영수증을 돌려주는 계약으로 바꿈 (audit:I08)
