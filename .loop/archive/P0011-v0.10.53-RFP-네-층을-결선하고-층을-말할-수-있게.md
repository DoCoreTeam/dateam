# PLAN newAX: RFP 네 층을 결선하고 층을 말할 수 있게
플랜 ID: P0011
플랜 버전: v0.1.1
상태: 완료
지시: ins_0007
목표 버전: v0.10.53
작성: 2026-09-16
시작 커밋: eabd7373

## 목표
- 사용자가 교차검증을 누르면 잡이 걸리고 202 를 받는데 실행기가 없어 그 잡은 반드시 실패한다
- 네 층(사실 검증 판단 생성) 중 어디서 난 값인지 말할 수 있게 한다
- 어긋난 값을 사실로 확정하지 않고 어긋남으로 표시한다

## 범위 밖
- 교차검증에 실제로 다른 모델을 부르는 일, 이번은 층을 나누고 안 도는 단계를 정리하는 것까지
- 적합도와 비교 단계 결선
- 공공데이터포털 (8단계)

## 완료 정의
- pnpm tsc --noEmit, pnpm lint, pnpm test, pnpm build 통과
- 걸 수 있는 잡은 전부 실행기가 있고, 없으면 걸기 전에 막힌다
- 값마다 어느 층에서 났는지 말할 수 있음
- 어긋난 값이 확정으로 안 올라감

## 참조
- docs/2026-09-10-ai-common-layer/README.md 7절 RFP 네 층
- apps/web/lib/rfp/jobs/run-stage.ts (붙지 않은 단계에서 던지는 자리)
- apps/web/lib/rfp/cross/consensus.ts (2층 판정이 이미 있는 자리)

## 항목

### I01 안 도는 단계가 만드는 실패를 재고 멈춘다
상태: 통과
모드: 중량
범위: apps/web/lib/rfp/jobs/stages.ts, apps/web/lib/rfp/jobs/run-stage.ts, apps/web/lib/rfp/jobs/run-stage.test.ts
감사 기준:
- 실측 정정을 notes 에 적음: DEFAULT_PIPELINE 은 analyze 에서 끝나므로 자동으로는 안 걸림, 처음에 매 건 실패한다고 읽은 것은 틀렸음
- 실제 결함: 사용자가 교차검증을 누르면 cross/route 가 잡을 걸고 202 를 돌려주는데 runStage 에 그 case 가 없어 그 잡은 반드시 실패함, 화면은 걸렸다고 말하고 결과는 안 옴
- 실행기가 있는 단계를 한 곳에 적고, 없는 단계를 걸려고 하면 걸기 전에 막음
- 202 를 받고 실패하는 것보다 400 을 받는 것이 낫다는 판단을 notes 에 적음
- 기존 run-stage 시험이 그대로 통과하고 새 단정이 늚
의존: 없음

### I02 값마다 어느 층에서 났는지 말한다
상태: 통과
모드: 경량
범위: apps/web/lib/rfp/domain/layer.ts (신규), apps/web/lib/rfp/domain/layer.test.ts (신규), apps/web/package.json
감사 기준:
- 층 넷(사실 검증 판단 생성)을 타입으로 두고 각 층이 어느 능력을 쓰는지 적음
- 값의 verification 과 grounding 으로 어느 층까지 갔는지 판정하는 함수
- 어긋난 값(conflict)은 확정으로 안 올라감, 단정으로 확인
- 경쟁 제한은 의심으로만 표기하고 특정 업체에 유리하다는 단정을 못 만들게 함
- node --test 통과, 등재 후 총 테스트 수 증가 확인
의존: I01

### I03 층 가드
상태: 통과
모드: 경량
범위: apps/web/lib/policy/rfp-layer-guard.test.ts (신규), apps/web/package.json
감사 기준:
- 파이프라인의 모든 단계에 실행기가 있는지 확인, 없으면 실패
- 특정 업체에 유리하다는 말이 코드에 있으면 실패
- 일부러 깨서 둘 다 확인
- 규칙이 도는 대상이 0개면 실패하는 단정
- 등재 후 총 테스트 수 증가 확인
의존: I02

## 종합 감사

실행한 명령과 결과 (2026-09-16)

- pnpm tsc --noEmit 통과, 오류 0건
- pnpm lint 통과, exit 0
- pnpm test 5604건 전부 통과, 실패 0
- pnpm build 통과 (힙 8192)

완료 정의 대조

- 걸 수 있는 잡은 전부 실행기가 있고, 없으면 걸기 전에 501 로 막힘
- 값마다 어느 층에서 났는지 말할 수 있음 (layerOf)
- 어긋난 값이 확정으로 안 올라감 (canConfirm)

전체 diff 검토

- 시작 커밋 eabd7373, 항목 커밋 3개 (v0.10.54 부터 v0.10.58)
- 범위 밖 변경 0건
- diff 에 키 토큰 비밀번호 0건

발견 사항 (계획에 없던 것 셋)

- 처음 읽기가 틀렸음: 매 분석마다 실패 잡이 남는 줄 알았으나 DEFAULT_PIPELINE 은 analyze 에서 끝남. 실제 결함은 사용자가 교차검증을 누를 때 202 를 받고 반드시 실패하는 것이었음
- 가드가 금지 문구를 적어 둔 주석 셋을 잡음. 규칙을 적는 것이 위반이 되면 아무도 규칙을 안 적으므로 주석과 부정문을 뺐음
- import 줄만 보고 통과하는 약점을 세 번째로 밟음. 실제 호출을 세도록 고침

남은 것

- 교차검증에 실제로 다른 모델을 부르는 실행기 (이번은 층을 나누고 걸린 척을 막는 것까지)
- 적합도와 비교 단계 실행기

## 변경 이력
- v0.1.0 (2026-09-16) 최초 작성 (ins_0007)
- v0.1.1 (2026-09-16) 자동으로 매번 실패하는 것이 아니라 사용자가 교차검증을 누를 때 202 를 받고 반드시 실패하는 것이었음 (audit:I01)
