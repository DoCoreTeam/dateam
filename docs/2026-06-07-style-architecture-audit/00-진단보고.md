# 스타일 아키텍처 파편화 진단 (구현 0 — 분석 전용)
> 2026-06-07 · 계기: NB 도입 시 "디자인 하나 바꾸기"가 codemod 수백 건을 요구한 이유 분석

## 핵심 수치
| 항목 | 수치 |
|------|------|
| 인라인 style={{}} 파일 비율 | 85.5% (106/124) |
| 인라인 style 총 건수 | 3,087건 |
| 하드코딩 hex 총 건수 | 2,243건 |
| 토큰(var(--)) 사용 비율 | 35.9% (나머지 64% 하드코딩) |
| Status 색 맵 중복 정의 파일 | 7개 |
| globals.css GPU 섹션 비중 | 29.5% (1,093/3,709줄) |
| 인디고 잔재 | 90건 |
| lint 강제 룰 | 0개 |

## 근본 원인 (왜 디자인 바꾸기가 힘든가)
1. **토큰 우회 64%** — :root 토큰 바꿔도 인라인 하드코딩 2,243건은 안 바뀜 → 변경이 화면에 전파 안 됨
2. **공용 컴포넌트 부재** — <Button>/<Card>/<Badge>/<Input>/<TableShell> 없음 → 버튼 스타일이 104파일에 분산
3. **Status 색 맵 7개 파일 중복** — done 색 하나 바꾸려면 7곳 동기화
4. **Tailwind ↔ CSS 토큰 불일치** — tailwind brand.500=#6366f1(인디고) vs --brand=#7c3aed(퍼플) → 유틸 못 믿어 hex 직접 씀(악순환)
5. **lint 강제 없음** — hex 직접 써도 빌드 통과
6. **globals.css 평면 3,709줄** — 페이지 전용(calendar/daily/gpu) 클automatedss 전역 혼재, !important 37건

## 의미 중복 색 (같은 의미 다른 hex)
- 성공: #16a34a/#10b981/#15803d/#059669/#22c55e … 8종
- 에러: #dc2626/#ef4444/#b91c1c … 7종
- 회색: #64748b/#94a3b8/#f1f5f9/#cbd5e1 …

## GPU 모듈 = 디자인 섬
전용 토큰 24 + 전용 클래스 203 + 1,093줄. NB와 6가지 충돌(보더색 #e9ebf0 vs #0a0a0a, 두께 1px vs 3px, radius 13px vs 4px, 강조 #5b5ef0 vs #7c3aed, 배경, 그림자). `.gpu-pricing-root`로 완전 스코핑 → NB 자동 침투 안 됨.
→ 권고: GPU는 의도적 예외 유지 + 강조색만 var(--brand) alias로 통일. 전면 NB는 디자인 의사결정 필요(단순 치환 아님).

## 캘린더 NB 미적용 잔여
- page.tsx 21-40 이벤트 색맵 하드코딩 / 216 헤더 #f1f5f9 / 246 뷰토글 hex / 560 today 분기 hex / 607 더보기뱃지 hex (총 93 hex)
- globals.css 1291-1521 .calendar-* (보더 1px #d8dee8 등 구 라이트보더)

## 통일 로드맵 (권고 — 구현 별도 승인 필요)
- **Phase 0** lib/tokens/status-colors.ts SSOT (7파일 → 1) ★즉효
- **Phase 1** eslint no-restricted-syntax로 인라인 hex 차단(신규부터)
- **Phase 2** 공용 컴포넌트 4종(<Button>/<Badge>/<Card>/<TableShell>) NB 내장
- **Phase 3** tailwind.config brand → var(--brand) 동기화
- **Phase 4** globals.css 모듈화(gpu/calendar/daily CSS 분리, 700줄로 축소)
- **Phase 5** 캘린더 NB 적용 + GPU 강조색 alias
