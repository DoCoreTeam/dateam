# 착수 전 결정 잠금 (§7 Q&A 종결)

> 2026-07-15 · CEO 확정(사용자 무응답 60s → 컨텍스트 최적 기본값, **전부 1줄 수정 가능**). 이 표가 구현 프롬프트 계약의 상위 SSOT.

| # | 결정 항목 | 확정값 | 근거 / 되돌리기 |
|---|---|---|---|
| Q1 | **배포 타깃** | **Vercel + 크론 드레인 워커** | 실측: `apps/web/vercel.json` 존재 + `maxDuration=300` 선례(`api/leads/parse`). 크론=이탈해도 진짜 백그라운드. 셀프호스트 전환 시 크론 대신 장수명 워커로 교체(오케스트레이터 인터페이스 동일). |
| Q2 | **취합 산출 형태** | **완성형 분석보고서**(MVP) | 사용자 "완성형" 어휘에 최부합. 원문 개정판은 v2.1로 이연(계약만 예약). |
| Q3 | **실시간 깊이** | **status 전이 + 열람 항목 text delta** | 균형점. 목록은 count(status) 파생 SSOT, 사용자가 연 항목만 토큰 delta. |
| Q4 | **lens 5종 거취** | **명령 프리셋 버튼으로 흡수** | 자유 명령(command)이 상위 개념. 기존 5 lens는 command 템플릿 프리셋으로 재배치(별도 분기 제거). |
| Q5 | **권한 / 동시성 K** | **admin 유지** · K = `org_content` META `ai_analysis_concurrency`(기본 4) | 현 AI채팅과 동일 경계, 비용 통제 용이. member 개방은 v2.2. K는 배포 없이 조절. |

**불변 제약(사용자 반복 강조 — 계약 최상위):**
- 유실0 / 생략0 — 취합은 AI 신뢰 금지, **코드 커버리지 게이트가 최종 판정**.
- 진행상태 하드코딩 금지 — `count(status)` 파생만.
- 이탈해도 안 죽음(백그라운드) + 사용자 임의 중단(취소·일시정지)은 **시스템 기본**.
- **구현 미착수** — 이 문서군은 기획 전용.
